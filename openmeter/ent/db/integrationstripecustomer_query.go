// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/openmeterio/openmeter/openmeter/ent/db/integrationstripecustomer"
	"github.com/openmeterio/openmeter/openmeter/ent/db/predicate"
)

// IntegrationStripeCustomerQuery is the builder for querying IntegrationStripeCustomer entities.
type IntegrationStripeCustomerQuery struct {
	config
	ctx        *QueryContext
	order      []integrationstripecustomer.OrderOption
	inters     []Interceptor
	predicates []predicate.IntegrationStripeCustomer
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the IntegrationStripeCustomerQuery builder.
func (iscq *IntegrationStripeCustomerQuery) Where(ps ...predicate.IntegrationStripeCustomer) *IntegrationStripeCustomerQuery {
	iscq.predicates = append(iscq.predicates, ps...)
	return iscq
}

// Limit the number of records to be returned by this query.
func (iscq *IntegrationStripeCustomerQuery) Limit(limit int) *IntegrationStripeCustomerQuery {
	iscq.ctx.Limit = &limit
	return iscq
}

// Offset to start from.
func (iscq *IntegrationStripeCustomerQuery) Offset(offset int) *IntegrationStripeCustomerQuery {
	iscq.ctx.Offset = &offset
	return iscq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (iscq *IntegrationStripeCustomerQuery) Unique(unique bool) *IntegrationStripeCustomerQuery {
	iscq.ctx.Unique = &unique
	return iscq
}

// Order specifies how the records should be ordered.
func (iscq *IntegrationStripeCustomerQuery) Order(o ...integrationstripecustomer.OrderOption) *IntegrationStripeCustomerQuery {
	iscq.order = append(iscq.order, o...)
	return iscq
}

// First returns the first IntegrationStripeCustomer entity from the query.
// Returns a *NotFoundError when no IntegrationStripeCustomer was found.
func (iscq *IntegrationStripeCustomerQuery) First(ctx context.Context) (*IntegrationStripeCustomer, error) {
	nodes, err := iscq.Limit(1).All(setContextOp(ctx, iscq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{integrationstripecustomer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (iscq *IntegrationStripeCustomerQuery) FirstX(ctx context.Context) *IntegrationStripeCustomer {
	node, err := iscq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first IntegrationStripeCustomer ID from the query.
// Returns a *NotFoundError when no IntegrationStripeCustomer ID was found.
func (iscq *IntegrationStripeCustomerQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = iscq.Limit(1).IDs(setContextOp(ctx, iscq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{integrationstripecustomer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (iscq *IntegrationStripeCustomerQuery) FirstIDX(ctx context.Context) int {
	id, err := iscq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single IntegrationStripeCustomer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one IntegrationStripeCustomer entity is found.
// Returns a *NotFoundError when no IntegrationStripeCustomer entities are found.
func (iscq *IntegrationStripeCustomerQuery) Only(ctx context.Context) (*IntegrationStripeCustomer, error) {
	nodes, err := iscq.Limit(2).All(setContextOp(ctx, iscq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{integrationstripecustomer.Label}
	default:
		return nil, &NotSingularError{integrationstripecustomer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (iscq *IntegrationStripeCustomerQuery) OnlyX(ctx context.Context) *IntegrationStripeCustomer {
	node, err := iscq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only IntegrationStripeCustomer ID in the query.
// Returns a *NotSingularError when more than one IntegrationStripeCustomer ID is found.
// Returns a *NotFoundError when no entities are found.
func (iscq *IntegrationStripeCustomerQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = iscq.Limit(2).IDs(setContextOp(ctx, iscq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{integrationstripecustomer.Label}
	default:
		err = &NotSingularError{integrationstripecustomer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (iscq *IntegrationStripeCustomerQuery) OnlyIDX(ctx context.Context) int {
	id, err := iscq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of IntegrationStripeCustomers.
func (iscq *IntegrationStripeCustomerQuery) All(ctx context.Context) ([]*IntegrationStripeCustomer, error) {
	ctx = setContextOp(ctx, iscq.ctx, ent.OpQueryAll)
	if err := iscq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*IntegrationStripeCustomer, *IntegrationStripeCustomerQuery]()
	return withInterceptors[[]*IntegrationStripeCustomer](ctx, iscq, qr, iscq.inters)
}

// AllX is like All, but panics if an error occurs.
func (iscq *IntegrationStripeCustomerQuery) AllX(ctx context.Context) []*IntegrationStripeCustomer {
	nodes, err := iscq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of IntegrationStripeCustomer IDs.
func (iscq *IntegrationStripeCustomerQuery) IDs(ctx context.Context) (ids []int, err error) {
	if iscq.ctx.Unique == nil && iscq.path != nil {
		iscq.Unique(true)
	}
	ctx = setContextOp(ctx, iscq.ctx, ent.OpQueryIDs)
	if err = iscq.Select(integrationstripecustomer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (iscq *IntegrationStripeCustomerQuery) IDsX(ctx context.Context) []int {
	ids, err := iscq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (iscq *IntegrationStripeCustomerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, iscq.ctx, ent.OpQueryCount)
	if err := iscq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, iscq, querierCount[*IntegrationStripeCustomerQuery](), iscq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (iscq *IntegrationStripeCustomerQuery) CountX(ctx context.Context) int {
	count, err := iscq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (iscq *IntegrationStripeCustomerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, iscq.ctx, ent.OpQueryExist)
	switch _, err := iscq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (iscq *IntegrationStripeCustomerQuery) ExistX(ctx context.Context) bool {
	exist, err := iscq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the IntegrationStripeCustomerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (iscq *IntegrationStripeCustomerQuery) Clone() *IntegrationStripeCustomerQuery {
	if iscq == nil {
		return nil
	}
	return &IntegrationStripeCustomerQuery{
		config:     iscq.config,
		ctx:        iscq.ctx.Clone(),
		order:      append([]integrationstripecustomer.OrderOption{}, iscq.order...),
		inters:     append([]Interceptor{}, iscq.inters...),
		predicates: append([]predicate.IntegrationStripeCustomer{}, iscq.predicates...),
		// clone intermediate query.
		sql:  iscq.sql.Clone(),
		path: iscq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Namespace string `json:"namespace,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.IntegrationStripeCustomer.Query().
//		GroupBy(integrationstripecustomer.FieldNamespace).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (iscq *IntegrationStripeCustomerQuery) GroupBy(field string, fields ...string) *IntegrationStripeCustomerGroupBy {
	iscq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &IntegrationStripeCustomerGroupBy{build: iscq}
	grbuild.flds = &iscq.ctx.Fields
	grbuild.label = integrationstripecustomer.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Namespace string `json:"namespace,omitempty"`
//	}
//
//	client.IntegrationStripeCustomer.Query().
//		Select(integrationstripecustomer.FieldNamespace).
//		Scan(ctx, &v)
func (iscq *IntegrationStripeCustomerQuery) Select(fields ...string) *IntegrationStripeCustomerSelect {
	iscq.ctx.Fields = append(iscq.ctx.Fields, fields...)
	sbuild := &IntegrationStripeCustomerSelect{IntegrationStripeCustomerQuery: iscq}
	sbuild.label = integrationstripecustomer.Label
	sbuild.flds, sbuild.scan = &iscq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a IntegrationStripeCustomerSelect configured with the given aggregations.
func (iscq *IntegrationStripeCustomerQuery) Aggregate(fns ...AggregateFunc) *IntegrationStripeCustomerSelect {
	return iscq.Select().Aggregate(fns...)
}

func (iscq *IntegrationStripeCustomerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range iscq.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, iscq); err != nil {
				return err
			}
		}
	}
	for _, f := range iscq.ctx.Fields {
		if !integrationstripecustomer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if iscq.path != nil {
		prev, err := iscq.path(ctx)
		if err != nil {
			return err
		}
		iscq.sql = prev
	}
	return nil
}

func (iscq *IntegrationStripeCustomerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*IntegrationStripeCustomer, error) {
	var (
		nodes = []*IntegrationStripeCustomer{}
		_spec = iscq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*IntegrationStripeCustomer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &IntegrationStripeCustomer{config: iscq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(iscq.modifiers) > 0 {
		_spec.Modifiers = iscq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, iscq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (iscq *IntegrationStripeCustomerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := iscq.querySpec()
	if len(iscq.modifiers) > 0 {
		_spec.Modifiers = iscq.modifiers
	}
	_spec.Node.Columns = iscq.ctx.Fields
	if len(iscq.ctx.Fields) > 0 {
		_spec.Unique = iscq.ctx.Unique != nil && *iscq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, iscq.driver, _spec)
}

func (iscq *IntegrationStripeCustomerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(integrationstripecustomer.Table, integrationstripecustomer.Columns, sqlgraph.NewFieldSpec(integrationstripecustomer.FieldID, field.TypeInt))
	_spec.From = iscq.sql
	if unique := iscq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if iscq.path != nil {
		_spec.Unique = true
	}
	if fields := iscq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, integrationstripecustomer.FieldID)
		for i := range fields {
			if fields[i] != integrationstripecustomer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := iscq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := iscq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := iscq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := iscq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (iscq *IntegrationStripeCustomerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(iscq.driver.Dialect())
	t1 := builder.Table(integrationstripecustomer.Table)
	columns := iscq.ctx.Fields
	if len(columns) == 0 {
		columns = integrationstripecustomer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if iscq.sql != nil {
		selector = iscq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if iscq.ctx.Unique != nil && *iscq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range iscq.modifiers {
		m(selector)
	}
	for _, p := range iscq.predicates {
		p(selector)
	}
	for _, p := range iscq.order {
		p(selector)
	}
	if offset := iscq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := iscq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (iscq *IntegrationStripeCustomerQuery) ForUpdate(opts ...sql.LockOption) *IntegrationStripeCustomerQuery {
	if iscq.driver.Dialect() == dialect.Postgres {
		iscq.Unique(false)
	}
	iscq.modifiers = append(iscq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return iscq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (iscq *IntegrationStripeCustomerQuery) ForShare(opts ...sql.LockOption) *IntegrationStripeCustomerQuery {
	if iscq.driver.Dialect() == dialect.Postgres {
		iscq.Unique(false)
	}
	iscq.modifiers = append(iscq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return iscq
}

// IntegrationStripeCustomerGroupBy is the group-by builder for IntegrationStripeCustomer entities.
type IntegrationStripeCustomerGroupBy struct {
	selector
	build *IntegrationStripeCustomerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (iscgb *IntegrationStripeCustomerGroupBy) Aggregate(fns ...AggregateFunc) *IntegrationStripeCustomerGroupBy {
	iscgb.fns = append(iscgb.fns, fns...)
	return iscgb
}

// Scan applies the selector query and scans the result into the given value.
func (iscgb *IntegrationStripeCustomerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, iscgb.build.ctx, ent.OpQueryGroupBy)
	if err := iscgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IntegrationStripeCustomerQuery, *IntegrationStripeCustomerGroupBy](ctx, iscgb.build, iscgb, iscgb.build.inters, v)
}

func (iscgb *IntegrationStripeCustomerGroupBy) sqlScan(ctx context.Context, root *IntegrationStripeCustomerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(iscgb.fns))
	for _, fn := range iscgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*iscgb.flds)+len(iscgb.fns))
		for _, f := range *iscgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*iscgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := iscgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// IntegrationStripeCustomerSelect is the builder for selecting fields of IntegrationStripeCustomer entities.
type IntegrationStripeCustomerSelect struct {
	*IntegrationStripeCustomerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (iscs *IntegrationStripeCustomerSelect) Aggregate(fns ...AggregateFunc) *IntegrationStripeCustomerSelect {
	iscs.fns = append(iscs.fns, fns...)
	return iscs
}

// Scan applies the selector query and scans the result into the given value.
func (iscs *IntegrationStripeCustomerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, iscs.ctx, ent.OpQuerySelect)
	if err := iscs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IntegrationStripeCustomerQuery, *IntegrationStripeCustomerSelect](ctx, iscs.IntegrationStripeCustomerQuery, iscs, iscs.inters, v)
}

func (iscs *IntegrationStripeCustomerSelect) sqlScan(ctx context.Context, root *IntegrationStripeCustomerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(iscs.fns))
	for _, fn := range iscs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*iscs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := iscs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
