// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/openmeterio/openmeter/openmeter/ent/db/billingledger"
	"github.com/openmeterio/openmeter/openmeter/ent/db/billingsubledger"
	"github.com/openmeterio/openmeter/openmeter/ent/db/billingsubledgertransaction"
	"github.com/openmeterio/openmeter/openmeter/ent/db/predicate"
)

// BillingSubledgerQuery is the builder for querying BillingSubledger entities.
type BillingSubledgerQuery struct {
	config
	ctx              *QueryContext
	order            []billingsubledger.OrderOption
	inters           []Interceptor
	predicates       []predicate.BillingSubledger
	withLedger       *BillingLedgerQuery
	withTransactions *BillingSubledgerTransactionQuery
	modifiers        []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BillingSubledgerQuery builder.
func (bsq *BillingSubledgerQuery) Where(ps ...predicate.BillingSubledger) *BillingSubledgerQuery {
	bsq.predicates = append(bsq.predicates, ps...)
	return bsq
}

// Limit the number of records to be returned by this query.
func (bsq *BillingSubledgerQuery) Limit(limit int) *BillingSubledgerQuery {
	bsq.ctx.Limit = &limit
	return bsq
}

// Offset to start from.
func (bsq *BillingSubledgerQuery) Offset(offset int) *BillingSubledgerQuery {
	bsq.ctx.Offset = &offset
	return bsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bsq *BillingSubledgerQuery) Unique(unique bool) *BillingSubledgerQuery {
	bsq.ctx.Unique = &unique
	return bsq
}

// Order specifies how the records should be ordered.
func (bsq *BillingSubledgerQuery) Order(o ...billingsubledger.OrderOption) *BillingSubledgerQuery {
	bsq.order = append(bsq.order, o...)
	return bsq
}

// QueryLedger chains the current query on the "ledger" edge.
func (bsq *BillingSubledgerQuery) QueryLedger() *BillingLedgerQuery {
	query := (&BillingLedgerClient{config: bsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := bsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := bsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(billingsubledger.Table, billingsubledger.FieldID, selector),
			sqlgraph.To(billingledger.Table, billingledger.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, billingsubledger.LedgerTable, billingsubledger.LedgerColumn),
		)
		fromU = sqlgraph.SetNeighbors(bsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTransactions chains the current query on the "transactions" edge.
func (bsq *BillingSubledgerQuery) QueryTransactions() *BillingSubledgerTransactionQuery {
	query := (&BillingSubledgerTransactionClient{config: bsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := bsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := bsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(billingsubledger.Table, billingsubledger.FieldID, selector),
			sqlgraph.To(billingsubledgertransaction.Table, billingsubledgertransaction.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, billingsubledger.TransactionsTable, billingsubledger.TransactionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(bsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BillingSubledger entity from the query.
// Returns a *NotFoundError when no BillingSubledger was found.
func (bsq *BillingSubledgerQuery) First(ctx context.Context) (*BillingSubledger, error) {
	nodes, err := bsq.Limit(1).All(setContextOp(ctx, bsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{billingsubledger.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bsq *BillingSubledgerQuery) FirstX(ctx context.Context) *BillingSubledger {
	node, err := bsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BillingSubledger ID from the query.
// Returns a *NotFoundError when no BillingSubledger ID was found.
func (bsq *BillingSubledgerQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = bsq.Limit(1).IDs(setContextOp(ctx, bsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{billingsubledger.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bsq *BillingSubledgerQuery) FirstIDX(ctx context.Context) string {
	id, err := bsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BillingSubledger entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BillingSubledger entity is found.
// Returns a *NotFoundError when no BillingSubledger entities are found.
func (bsq *BillingSubledgerQuery) Only(ctx context.Context) (*BillingSubledger, error) {
	nodes, err := bsq.Limit(2).All(setContextOp(ctx, bsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{billingsubledger.Label}
	default:
		return nil, &NotSingularError{billingsubledger.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bsq *BillingSubledgerQuery) OnlyX(ctx context.Context) *BillingSubledger {
	node, err := bsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BillingSubledger ID in the query.
// Returns a *NotSingularError when more than one BillingSubledger ID is found.
// Returns a *NotFoundError when no entities are found.
func (bsq *BillingSubledgerQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = bsq.Limit(2).IDs(setContextOp(ctx, bsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{billingsubledger.Label}
	default:
		err = &NotSingularError{billingsubledger.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bsq *BillingSubledgerQuery) OnlyIDX(ctx context.Context) string {
	id, err := bsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BillingSubledgers.
func (bsq *BillingSubledgerQuery) All(ctx context.Context) ([]*BillingSubledger, error) {
	ctx = setContextOp(ctx, bsq.ctx, ent.OpQueryAll)
	if err := bsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BillingSubledger, *BillingSubledgerQuery]()
	return withInterceptors[[]*BillingSubledger](ctx, bsq, qr, bsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bsq *BillingSubledgerQuery) AllX(ctx context.Context) []*BillingSubledger {
	nodes, err := bsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BillingSubledger IDs.
func (bsq *BillingSubledgerQuery) IDs(ctx context.Context) (ids []string, err error) {
	if bsq.ctx.Unique == nil && bsq.path != nil {
		bsq.Unique(true)
	}
	ctx = setContextOp(ctx, bsq.ctx, ent.OpQueryIDs)
	if err = bsq.Select(billingsubledger.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bsq *BillingSubledgerQuery) IDsX(ctx context.Context) []string {
	ids, err := bsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bsq *BillingSubledgerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bsq.ctx, ent.OpQueryCount)
	if err := bsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bsq, querierCount[*BillingSubledgerQuery](), bsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bsq *BillingSubledgerQuery) CountX(ctx context.Context) int {
	count, err := bsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bsq *BillingSubledgerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bsq.ctx, ent.OpQueryExist)
	switch _, err := bsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bsq *BillingSubledgerQuery) ExistX(ctx context.Context) bool {
	exist, err := bsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BillingSubledgerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bsq *BillingSubledgerQuery) Clone() *BillingSubledgerQuery {
	if bsq == nil {
		return nil
	}
	return &BillingSubledgerQuery{
		config:           bsq.config,
		ctx:              bsq.ctx.Clone(),
		order:            append([]billingsubledger.OrderOption{}, bsq.order...),
		inters:           append([]Interceptor{}, bsq.inters...),
		predicates:       append([]predicate.BillingSubledger{}, bsq.predicates...),
		withLedger:       bsq.withLedger.Clone(),
		withTransactions: bsq.withTransactions.Clone(),
		// clone intermediate query.
		sql:  bsq.sql.Clone(),
		path: bsq.path,
	}
}

// WithLedger tells the query-builder to eager-load the nodes that are connected to
// the "ledger" edge. The optional arguments are used to configure the query builder of the edge.
func (bsq *BillingSubledgerQuery) WithLedger(opts ...func(*BillingLedgerQuery)) *BillingSubledgerQuery {
	query := (&BillingLedgerClient{config: bsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	bsq.withLedger = query
	return bsq
}

// WithTransactions tells the query-builder to eager-load the nodes that are connected to
// the "transactions" edge. The optional arguments are used to configure the query builder of the edge.
func (bsq *BillingSubledgerQuery) WithTransactions(opts ...func(*BillingSubledgerTransactionQuery)) *BillingSubledgerQuery {
	query := (&BillingSubledgerTransactionClient{config: bsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	bsq.withTransactions = query
	return bsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Namespace string `json:"namespace,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BillingSubledger.Query().
//		GroupBy(billingsubledger.FieldNamespace).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (bsq *BillingSubledgerQuery) GroupBy(field string, fields ...string) *BillingSubledgerGroupBy {
	bsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BillingSubledgerGroupBy{build: bsq}
	grbuild.flds = &bsq.ctx.Fields
	grbuild.label = billingsubledger.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Namespace string `json:"namespace,omitempty"`
//	}
//
//	client.BillingSubledger.Query().
//		Select(billingsubledger.FieldNamespace).
//		Scan(ctx, &v)
func (bsq *BillingSubledgerQuery) Select(fields ...string) *BillingSubledgerSelect {
	bsq.ctx.Fields = append(bsq.ctx.Fields, fields...)
	sbuild := &BillingSubledgerSelect{BillingSubledgerQuery: bsq}
	sbuild.label = billingsubledger.Label
	sbuild.flds, sbuild.scan = &bsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BillingSubledgerSelect configured with the given aggregations.
func (bsq *BillingSubledgerQuery) Aggregate(fns ...AggregateFunc) *BillingSubledgerSelect {
	return bsq.Select().Aggregate(fns...)
}

func (bsq *BillingSubledgerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bsq.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bsq); err != nil {
				return err
			}
		}
	}
	for _, f := range bsq.ctx.Fields {
		if !billingsubledger.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if bsq.path != nil {
		prev, err := bsq.path(ctx)
		if err != nil {
			return err
		}
		bsq.sql = prev
	}
	return nil
}

func (bsq *BillingSubledgerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BillingSubledger, error) {
	var (
		nodes       = []*BillingSubledger{}
		_spec       = bsq.querySpec()
		loadedTypes = [2]bool{
			bsq.withLedger != nil,
			bsq.withTransactions != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BillingSubledger).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BillingSubledger{config: bsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(bsq.modifiers) > 0 {
		_spec.Modifiers = bsq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := bsq.withLedger; query != nil {
		if err := bsq.loadLedger(ctx, query, nodes, nil,
			func(n *BillingSubledger, e *BillingLedger) { n.Edges.Ledger = e }); err != nil {
			return nil, err
		}
	}
	if query := bsq.withTransactions; query != nil {
		if err := bsq.loadTransactions(ctx, query, nodes,
			func(n *BillingSubledger) { n.Edges.Transactions = []*BillingSubledgerTransaction{} },
			func(n *BillingSubledger, e *BillingSubledgerTransaction) {
				n.Edges.Transactions = append(n.Edges.Transactions, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (bsq *BillingSubledgerQuery) loadLedger(ctx context.Context, query *BillingLedgerQuery, nodes []*BillingSubledger, init func(*BillingSubledger), assign func(*BillingSubledger, *BillingLedger)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*BillingSubledger)
	for i := range nodes {
		fk := nodes[i].LedgerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(billingledger.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "ledger_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (bsq *BillingSubledgerQuery) loadTransactions(ctx context.Context, query *BillingSubledgerTransactionQuery, nodes []*BillingSubledger, init func(*BillingSubledger), assign func(*BillingSubledger, *BillingSubledgerTransaction)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*BillingSubledger)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(billingsubledgertransaction.FieldSubledgerID)
	}
	query.Where(predicate.BillingSubledgerTransaction(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(billingsubledger.TransactionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SubledgerID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "subledger_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (bsq *BillingSubledgerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bsq.querySpec()
	if len(bsq.modifiers) > 0 {
		_spec.Modifiers = bsq.modifiers
	}
	_spec.Node.Columns = bsq.ctx.Fields
	if len(bsq.ctx.Fields) > 0 {
		_spec.Unique = bsq.ctx.Unique != nil && *bsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bsq.driver, _spec)
}

func (bsq *BillingSubledgerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(billingsubledger.Table, billingsubledger.Columns, sqlgraph.NewFieldSpec(billingsubledger.FieldID, field.TypeString))
	_spec.From = bsq.sql
	if unique := bsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bsq.path != nil {
		_spec.Unique = true
	}
	if fields := bsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, billingsubledger.FieldID)
		for i := range fields {
			if fields[i] != billingsubledger.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if bsq.withLedger != nil {
			_spec.Node.AddColumnOnce(billingsubledger.FieldLedgerID)
		}
	}
	if ps := bsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bsq *BillingSubledgerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bsq.driver.Dialect())
	t1 := builder.Table(billingsubledger.Table)
	columns := bsq.ctx.Fields
	if len(columns) == 0 {
		columns = billingsubledger.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bsq.sql != nil {
		selector = bsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bsq.ctx.Unique != nil && *bsq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range bsq.modifiers {
		m(selector)
	}
	for _, p := range bsq.predicates {
		p(selector)
	}
	for _, p := range bsq.order {
		p(selector)
	}
	if offset := bsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (bsq *BillingSubledgerQuery) ForUpdate(opts ...sql.LockOption) *BillingSubledgerQuery {
	if bsq.driver.Dialect() == dialect.Postgres {
		bsq.Unique(false)
	}
	bsq.modifiers = append(bsq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return bsq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (bsq *BillingSubledgerQuery) ForShare(opts ...sql.LockOption) *BillingSubledgerQuery {
	if bsq.driver.Dialect() == dialect.Postgres {
		bsq.Unique(false)
	}
	bsq.modifiers = append(bsq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return bsq
}

// BillingSubledgerGroupBy is the group-by builder for BillingSubledger entities.
type BillingSubledgerGroupBy struct {
	selector
	build *BillingSubledgerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bsgb *BillingSubledgerGroupBy) Aggregate(fns ...AggregateFunc) *BillingSubledgerGroupBy {
	bsgb.fns = append(bsgb.fns, fns...)
	return bsgb
}

// Scan applies the selector query and scans the result into the given value.
func (bsgb *BillingSubledgerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bsgb.build.ctx, ent.OpQueryGroupBy)
	if err := bsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BillingSubledgerQuery, *BillingSubledgerGroupBy](ctx, bsgb.build, bsgb, bsgb.build.inters, v)
}

func (bsgb *BillingSubledgerGroupBy) sqlScan(ctx context.Context, root *BillingSubledgerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bsgb.fns))
	for _, fn := range bsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bsgb.flds)+len(bsgb.fns))
		for _, f := range *bsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BillingSubledgerSelect is the builder for selecting fields of BillingSubledger entities.
type BillingSubledgerSelect struct {
	*BillingSubledgerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bss *BillingSubledgerSelect) Aggregate(fns ...AggregateFunc) *BillingSubledgerSelect {
	bss.fns = append(bss.fns, fns...)
	return bss
}

// Scan applies the selector query and scans the result into the given value.
func (bss *BillingSubledgerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bss.ctx, ent.OpQuerySelect)
	if err := bss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BillingSubledgerQuery, *BillingSubledgerSelect](ctx, bss.BillingSubledgerQuery, bss, bss.inters, v)
}

func (bss *BillingSubledgerSelect) sqlScan(ctx context.Context, root *BillingSubledgerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bss.fns))
	for _, fn := range bss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
