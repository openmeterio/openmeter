name: Nix Cache Reproduction

on:
  pull_request:

permissions:
  contents: read

jobs:
  publish:
    name: Publish isolated Nix cache
    runs-on: depot-ubuntu-latest-4

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Nix
        uses: nixbuild/nix-quick-install-action@9f63be77f412a248c9d9a65a4c82cf066cdf8f0c # v35
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
          nix_conf: |
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
            keep-env-derivations = true
            keep-outputs = true

      - name: Realize Nix environment
        run: nix develop --impure .#ci

      - name: Verify Go runtime loader
        run: |
          runtime_dir="${RUNNER_TEMP}/openmeter-go-runtime"
          mkdir -p "${runtime_dir}/go-tmp"

          GOTMPDIR="${runtime_dir}/go-tmp" nix develop --impure .#ci -c bash -euo pipefail <<'EOF'
            runtime_probe="${RUNNER_TEMP}/openmeter-go-runtime/client.test"
            expected_interpreter="$(cat "${NIX_CC}/nix-support/dynamic-linker")"
            go env -json GOCACHE GOROOT GOTOOLDIR
            printf 'GO_LDSO=%s\n' "${GO_LDSO:-<unset>}"

            if [[ "${GO_LDSO:-}" != "${expected_interpreter}" ]]; then
              echo "GO_LDSO uses ${GO_LDSO:-<unset>}; expected ${expected_interpreter}"
              exit 1
            fi

            # GO_LDSO is not part of Go's link-action key. Start publication from
            # an empty namespace so the assertion exercises the wrapped linker.
            go clean -cache
            go -C api/v3/client test -c -o "${runtime_probe}" .

            # A cached Go link action can retain the loader from an older Nixpkgs
            # generation. Never publish a cache containing such an executable.
            interpreter="$(readelf -l "${runtime_probe}" | sed -n 's/.*interpreter: \(.*\)]/\1/p')"
            if [[ "${interpreter}" != "${expected_interpreter}" ]]; then
              echo "Go runtime probe uses ${interpreter:-<none>}; expected ${expected_interpreter}"
              exit 1
            fi

            "${runtime_probe}" -test.run '^$'
          EOF

      - name: Save isolated Nix store cache
        uses: nix-community/cache-nix-action/save@7df957e333c1e5da7721f60227dbba6d06080569 # v7.0.2
        with:
          primary-key: ${{ runner.os }}-openmeter-nix-build-v2-${{ github.ref_name }}-${{ hashFiles('flake.*') }}
          save: "true"

  restore-and-generate:
    name: Restore isolated Nix cache and run Go generate
    needs: publish
    runs-on: depot-ubuntu-latest-8

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Nix
        uses: nixbuild/nix-quick-install-action@9f63be77f412a248c9d9a65a4c82cf066cdf8f0c # v35
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
          nix_conf: |
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
            keep-env-derivations = true
            keep-outputs = true

      - name: Restore isolated Nix store cache
        uses: nix-community/cache-nix-action/restore@7df957e333c1e5da7721f60227dbba6d06080569 # v7.0.2
        with:
          primary-key: ${{ runner.os }}-openmeter-nix-build-v2-${{ github.ref_name }}-${{ hashFiles('flake.*') }}
          fail-on: primary-key.not-restored
          save: "false"

      - name: Probe dynamically linked Go executable
        env:
          GOTMPDIR: ${{ github.workspace }}/.cache-diagnostics/go-tmp
          TMPDIR: ${{ github.workspace }}/.cache-diagnostics/tmp
        run: |
          mkdir -p "${GOTMPDIR}" "${TMPDIR}"
          nix develop --impure .#ci -c bash -euo pipefail <<'EOF'
            probe_source="$(mktemp --suffix=.go)"
            probe_binary="$(mktemp)"
            printf "%s\n" \
              "package main" \
              "/* #include <stdlib.h> */" \
              "import \"C\"" \
              "func main() {}" > "${probe_source}"
            go build -o "${probe_binary}" "${probe_source}"
            file "${probe_binary}"
            readelf -l "${probe_binary}" | grep "Requesting program interpreter"
            "${probe_binary}"
          EOF

      - name: Inspect representative Go binaries
        env:
          GOTMPDIR: ${{ github.workspace }}/.cache-diagnostics/go-tmp
          TMPDIR: ${{ github.workspace }}/.cache-diagnostics/tmp
        run: |
          mkdir -p "${GOTMPDIR}" "${TMPDIR}"
          nix develop --impure .#ci -c bash -euo pipefail <<'EOF'
            expected_interpreter="$(cat "${NIX_CC}/nix-support/dynamic-linker")"

            inspect_binary() {
              local label="$1"
              local binary="$2"
              shift 2

              echo "::group::${label}"
              file "${binary}"
              go version -m "${binary}" || true

              local interpreter
              interpreter="$(readelf -l "${binary}" | sed -n 's/.*interpreter: \(.*\)]/\1/p')"
              printf 'interpreter=%s\n' "${interpreter:-<none>}"

              if [ -n "${interpreter}" ]; then
                # Fail on stale link results even if their old loader happens to
                # be present in the restored Nix store and the binary can run.
                if [ "${interpreter}" != "${expected_interpreter}" ]; then
                  echo "${label} uses ${interpreter}; expected ${expected_interpreter}"
                  exit 1
                fi

                ls -la "${interpreter}" || true

                local store_name="${interpreter#/nix/store/}"
                local store_path="/nix/store/${store_name%%/*}"
                nix-store --verify-path "${store_path}" || true
              fi

              set +e
              "${binary}" "$@"
              local exit_code=$?
              set -e
              printf 'exit_code=%s\n' "${exit_code}"
              echo "::endgroup::"

              return "${exit_code}"
            }

            go env GOOS GOARCH GOFLAGS CGO_ENABLED CC CXX GOCACHE
            printf 'GO_LDSO=%s\n' "${GO_LDSO:-<unset>}"
            printf 'NIX_CC=%s\n' "${NIX_CC:-<unset>}"
            if [ -r "${NIX_CC}/nix-support/dynamic-linker" ]; then
              printf 'active_dynamic_linker='
              cat "${NIX_CC}/nix-support/dynamic-linker"
            fi

            client_test="$(mktemp)"
            go -C api/v3/client test -c -o "${client_test}" .
            inspect_binary "Go SDK test binary" "${client_test}" -test.run '^$'

            oapi_codegen="$(mktemp)"
            go build -o "${oapi_codegen}" github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen
            inspect_binary "oapi-codegen" "${oapi_codegen}" -version || true

            entc="$(mktemp)"
            go build -o "${entc}" ./openmeter/ent/entc.go
            inspect_binary "entc" "${entc}" -h || true
          EOF

      - name: Run Go generators
        env:
          GOTMPDIR: ${{ github.workspace }}/.cache-diagnostics/go-tmp
          TMPDIR: ${{ github.workspace }}/.cache-diagnostics/tmp
        run: |
          mkdir -p "${GOTMPDIR}" "${TMPDIR}"
          # GOCACHE deliberately comes from the Nix shell: flake.nix namespaces it
          # by the complete Go and runtime-loader store identities.
          nix develop --impure .#ci -c make patch-oapi-templates
          nix develop --impure .#ci -c go generate ./...
