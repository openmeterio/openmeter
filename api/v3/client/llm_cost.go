// Code generated by @openmeter/typespec-go. DO NOT EDIT.

package openmeter

import (
	"context"
	"fmt"
	"iter"
	"net/http"
	"net/url"
)

type LLMCostService struct {
	client *Client
}

type ListLLMCostPricesFilter struct {
	// Filter by provider. e.g. ?filter[provider][eq]=openai
	Provider *StringFilter
	// Filter by model ID. e.g. ?filter[model_id][eq]=gpt-4
	ModelID *StringFilter
	// Filter by model name. e.g. ?filter[model_name][contains]=gpt
	ModelName *StringFilter
	// Filter by currency code. e.g. ?filter[currency][eq]=USD
	Currency *StringFilter
	// Filter by source. e.g. ?filter[source][eq]=system
	Source *StringFilter
}

type ListLLMCostPricesParams struct {
	Filter *ListLLMCostPricesFilter
	Sort   *Sort
	Page   *PageParams
}

func (p ListLLMCostPricesParams) values() url.Values {
	q := url.Values{}

	if p.Filter != nil {
		addStringFilter(q, "filter[provider]", p.Filter.Provider)
		addStringFilter(q, "filter[model_id]", p.Filter.ModelID)
		addStringFilter(q, "filter[model_name]", p.Filter.ModelName)
		addStringFilter(q, "filter[currency]", p.Filter.Currency)
		addStringFilter(q, "filter[source]", p.Filter.Source)
	}

	addSort(q, "sort", p.Sort)

	addPageParams(q, p.Page)

	return q
}

type ListLLMCostOverridesFilter struct {
	// Filter by provider. e.g. ?filter[provider][eq]=openai
	Provider *StringFilter
	// Filter by model ID. e.g. ?filter[model_id][eq]=gpt-4
	ModelID *StringFilter
	// Filter by model name. e.g. ?filter[model_name][contains]=gpt
	ModelName *StringFilter
	// Filter by currency code. e.g. ?filter[currency][eq]=USD
	Currency *StringFilter
	// Filter by source. e.g. ?filter[source][eq]=system
	Source *StringFilter
}

type ListLLMCostOverridesParams struct {
	Filter *ListLLMCostOverridesFilter
	Page   *PageParams
}

func (p ListLLMCostOverridesParams) values() url.Values {
	q := url.Values{}

	if p.Filter != nil {
		addStringFilter(q, "filter[provider]", p.Filter.Provider)
		addStringFilter(q, "filter[model_id]", p.Filter.ModelID)
		addStringFilter(q, "filter[model_name]", p.Filter.ModelName)
		addStringFilter(q, "filter[currency]", p.Filter.Currency)
		addStringFilter(q, "filter[source]", p.Filter.Source)
	}

	addPageParams(q, p.Page)

	return q
}

// List global LLM cost prices. Returns prices with overrides applied if any.
func (s *LLMCostService) ListPrices(ctx context.Context, params ListLLMCostPricesParams) (*PricePagePaginatedResponse, error) {
	path := "/openmeter/llm-cost/prices"

	req, err := s.client.newRequestWithContentType(ctx, http.MethodGet, path, params.values(), nil, "", "application/json")
	if err != nil {
		return nil, err
	}

	var out PricePagePaginatedResponse
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// ListPricesAll returns an iterator over all LLMCostPrice results, fetching pages of ListPrices transparently. Iteration stops at the first error, which is yielded as the second value.
func (s *LLMCostService) ListPricesAll(ctx context.Context, params ListLLMCostPricesParams) iter.Seq2[LLMCostPrice, error] {
	return paginate(params.Page, func(page, size int) ([]LLMCostPrice, int, error) {
		pageParams := params
		pageParams.Page = &PageParams{Size: Int(size), Number: Int(page)}

		resp, err := s.ListPrices(ctx, pageParams)
		if err != nil {
			return nil, 0, err
		}

		return resp.Data, resp.Meta.Page.Total, nil
	})
}

// Get a specific LLM cost price by ID. Returns the price with overrides applied if
// any.
func (s *LLMCostService) GetPrice(ctx context.Context, priceID string) (*LLMCostPrice, error) {
	if priceID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "priceID", ErrEmptyID)
	}

	path := "/openmeter/llm-cost/prices/{priceId}"

	path = replacePathParam(path, "priceId", priceID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodGet, path, nil, nil, "", "application/json")
	if err != nil {
		return nil, err
	}

	var out LLMCostPrice
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// List per-namespace price overrides.
func (s *LLMCostService) ListOverrides(ctx context.Context, params ListLLMCostOverridesParams) (*PricePagePaginatedResponse, error) {
	path := "/openmeter/llm-cost/overrides"

	req, err := s.client.newRequestWithContentType(ctx, http.MethodGet, path, params.values(), nil, "", "application/json")
	if err != nil {
		return nil, err
	}

	var out PricePagePaginatedResponse
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// ListOverridesAll returns an iterator over all LLMCostPrice results, fetching pages of ListOverrides transparently. Iteration stops at the first error, which is yielded as the second value.
func (s *LLMCostService) ListOverridesAll(ctx context.Context, params ListLLMCostOverridesParams) iter.Seq2[LLMCostPrice, error] {
	return paginate(params.Page, func(page, size int) ([]LLMCostPrice, int, error) {
		pageParams := params
		pageParams.Page = &PageParams{Size: Int(size), Number: Int(page)}

		resp, err := s.ListOverrides(ctx, pageParams)
		if err != nil {
			return nil, 0, err
		}

		return resp.Data, resp.Meta.Page.Total, nil
	})
}

// Create a per-namespace price override.
func (s *LLMCostService) CreateOverride(ctx context.Context, request LLMCostOverrideCreate) (*LLMCostPrice, error) {
	path := "/openmeter/llm-cost/overrides"

	req, err := s.client.newRequestWithContentType(ctx, http.MethodPost, path, nil, request, "application/json", "application/json")
	if err != nil {
		return nil, err
	}

	var out LLMCostPrice
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// Delete a per-namespace price override.
func (s *LLMCostService) DeleteOverride(ctx context.Context, priceID string) error {
	if priceID == "" {
		return fmt.Errorf("openmeter: %s must not be empty: %w", "priceID", ErrEmptyID)
	}

	path := "/openmeter/llm-cost/overrides/{priceId}"

	path = replacePathParam(path, "priceId", priceID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodDelete, path, nil, nil, "", "")
	if err != nil {
		return err
	}

	_, err = s.client.doRaw(req)
	return err
}
