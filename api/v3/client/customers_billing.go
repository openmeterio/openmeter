// Code generated by @openmeter/typespec-go. DO NOT EDIT.

package openmeter

import (
	"context"
	"fmt"
	"net/http"
)

type CustomersBillingService struct {
	client *Client
}

func (s *CustomersBillingService) Get(ctx context.Context, customerID string) (*CustomerData, error) {
	if customerID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "customerID", ErrEmptyID)
	}

	path := "/openmeter/customers/{customerId}/billing"

	path = replacePathParam(path, "customerId", customerID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodGet, path, nil, nil, "", "application/json")
	if err != nil {
		return nil, err
	}

	var out CustomerData
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

func (s *CustomersBillingService) Update(ctx context.Context, customerID string, request UpsertCustomerBillingDataRequest) (*CustomerData, error) {
	if customerID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "customerID", ErrEmptyID)
	}

	path := "/openmeter/customers/{customerId}/billing"

	path = replacePathParam(path, "customerId", customerID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodPut, path, nil, request, "application/json", "application/json")
	if err != nil {
		return nil, err
	}

	var out CustomerData
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

func (s *CustomersBillingService) UpdateAppData(ctx context.Context, customerID string, request UpsertAppCustomerDataRequest) (*AppCustomerData, error) {
	if customerID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "customerID", ErrEmptyID)
	}

	path := "/openmeter/customers/{customerId}/billing/app-data"

	path = replacePathParam(path, "customerId", customerID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodPut, path, nil, request, "application/json", "application/json")
	if err != nil {
		return nil, err
	}

	var out AppCustomerData
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// Create a [Stripe Checkout Session](https://docs.stripe.com/payments/checkout)
// for the customer.
//
// Creates a Checkout Session for collecting payment method information from
// customers. The session operates in "setup" mode, which collects payment details
// without charging the customer immediately. The collected payment method can be
// used for future subscription billing.
//
// For hosted checkout sessions, redirect customers to the returned URL. For
// embedded sessions, use the client_secret to initialize Stripe.js in your
// application.
func (s *CustomersBillingService) CreateStripeCheckoutSession(ctx context.Context, customerID string, request CustomerStripeCreateCheckoutSessionRequest) (*AppStripeCreateCheckoutSessionResult, error) {
	if customerID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "customerID", ErrEmptyID)
	}

	path := "/openmeter/customers/{customerId}/billing/stripe/checkout-sessions"

	path = replacePathParam(path, "customerId", customerID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodPost, path, nil, request, "application/json", "application/json")
	if err != nil {
		return nil, err
	}

	var out AppStripeCreateCheckoutSessionResult
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// Create Stripe Customer Portal Session.
//
// Useful to redirect the customer to the Stripe Customer Portal to manage their
// payment methods, change their billing address and access their invoice history.
// Only returns URL if the customer billing profile is linked to a stripe app and
// customer.
func (s *CustomersBillingService) CreateStripePortalSession(ctx context.Context, customerID string, request CustomerStripeCreateCustomerPortalSessionRequest) (*AppStripeCreateCustomerPortalSessionResult, error) {
	if customerID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "customerID", ErrEmptyID)
	}

	path := "/openmeter/customers/{customerId}/billing/stripe/portal-sessions"

	path = replacePathParam(path, "customerId", customerID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodPost, path, nil, request, "application/json", "application/json")
	if err != nil {
		return nil, err
	}

	var out AppStripeCreateCustomerPortalSessionResult
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}
