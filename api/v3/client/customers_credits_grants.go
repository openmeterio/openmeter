// Code generated by @openmeter/typespec-go. DO NOT EDIT.

package openmeter

import (
	"context"
	"fmt"
	"iter"
	"net/http"
	"net/url"
)

type CustomersCreditsGrantsService struct {
	client *Client
}

type CreditGrantFilter struct {
	// Filter credit grants by status.
	Status *CreditGrantStatus
	// Filter credit grants by currency.
	Currency *BillingCurrencyCode
	// Filter credit grants by key.
	Key *StringFilter
}

type CreditGrantListParams struct {
	Page   *PageParams
	Filter *CreditGrantFilter
}

func (p CreditGrantListParams) values() url.Values {
	q := url.Values{}

	addPageParams(q, p.Page)

	if p.Filter != nil {
		if p.Filter.Status != nil {
			q.Set("filter[status]", string(*p.Filter.Status))
		}
		if p.Filter.Currency != nil {
			q.Set("filter[currency]", string(*p.Filter.Currency))
		}
		addStringFilter(q, "filter[key]", p.Filter.Key)
	}

	return q
}

// Create a new credit grant. A credit grant represents an allocation of prepaid
// credits to a customer.
func (s *CustomersCreditsGrantsService) Create(ctx context.Context, customerID string, request CreateCreditGrantRequest) (*CreditGrant, error) {
	if customerID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "customerID", ErrEmptyID)
	}

	path := "/openmeter/customers/{customerId}/credits/grants"

	path = replacePathParam(path, "customerId", customerID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodPost, path, nil, request, "application/json", "application/json")
	if err != nil {
		return nil, err
	}

	var out CreditGrant
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// Get a credit grant.
func (s *CustomersCreditsGrantsService) Get(ctx context.Context, customerID string, creditGrantID string) (*CreditGrant, error) {
	if customerID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "customerID", ErrEmptyID)
	}

	if creditGrantID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "creditGrantID", ErrEmptyID)
	}

	path := "/openmeter/customers/{customerId}/credits/grants/{creditGrantId}"

	path = replacePathParam(path, "customerId", customerID)

	path = replacePathParam(path, "creditGrantId", creditGrantID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodGet, path, nil, nil, "", "application/json")
	if err != nil {
		return nil, err
	}

	var out CreditGrant
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// List credit grants.
func (s *CustomersCreditsGrantsService) List(ctx context.Context, customerID string, params CreditGrantListParams) (*CreditGrantPagePaginatedResponse, error) {
	if customerID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "customerID", ErrEmptyID)
	}

	path := "/openmeter/customers/{customerId}/credits/grants"

	path = replacePathParam(path, "customerId", customerID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodGet, path, params.values(), nil, "", "application/json")
	if err != nil {
		return nil, err
	}

	var out CreditGrantPagePaginatedResponse
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// ListAll returns an iterator over all CreditGrant results, fetching pages of List transparently. Iteration stops at the first error, which is yielded as the second value.
func (s *CustomersCreditsGrantsService) ListAll(ctx context.Context, customerID string, params CreditGrantListParams) iter.Seq2[CreditGrant, error] {
	return paginate(params.Page, func(page, size int) ([]CreditGrant, int, error) {
		pageParams := params
		pageParams.Page = &PageParams{Size: Int(size), Number: Int(page)}

		resp, err := s.List(ctx, customerID, pageParams)
		if err != nil {
			return nil, 0, err
		}

		return resp.Data, resp.Meta.Page.Total, nil
	})
}

// Update the payment settlement status of an externally funded credit grant.
//
// Use this endpoint to synchronize the payment state of an external payment with
// the system so that revenue recognition and credit availability work as expected.
func (s *CustomersCreditsGrantsService) UpdateExternalSettlement(ctx context.Context, customerID string, creditGrantID string, request UpdateCreditGrantExternalSettlementRequest) (*CreditGrant, error) {
	if customerID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "customerID", ErrEmptyID)
	}

	if creditGrantID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "creditGrantID", ErrEmptyID)
	}

	path := "/openmeter/customers/{customerId}/credits/grants/{creditGrantId}/settlement/external"

	path = replacePathParam(path, "customerId", customerID)

	path = replacePathParam(path, "creditGrantId", creditGrantID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodPost, path, nil, request, "application/json", "application/json")
	if err != nil {
		return nil, err
	}

	var out CreditGrant
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}
