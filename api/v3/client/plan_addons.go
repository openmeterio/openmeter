// Code generated by @openmeter/typespec-go. DO NOT EDIT.

package openmeter

import (
	"context"
	"fmt"
	"iter"
	"net/http"
	"net/url"
)

type PlanAddonsService struct {
	client *Client
}

type PlanAddonListParams struct {
	Page *PageParams
}

func (p PlanAddonListParams) values() url.Values {
	q := url.Values{}

	addPageParams(q, p.Page)

	return q
}

// List add-ons associated with a plan.
func (s *PlanAddonsService) List(ctx context.Context, planID string, params PlanAddonListParams) (*PlanAddonPagePaginatedResponse, error) {
	if planID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "planID", ErrEmptyID)
	}

	path := "/openmeter/plans/{planId}/addons"

	path = replacePathParam(path, "planId", planID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodGet, path, params.values(), nil, "", "application/json")
	if err != nil {
		return nil, err
	}

	var out PlanAddonPagePaginatedResponse
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// ListAll returns an iterator over all PlanAddon results, fetching pages of List transparently. Iteration stops at the first error, which is yielded as the second value.
func (s *PlanAddonsService) ListAll(ctx context.Context, planID string, params PlanAddonListParams) iter.Seq2[PlanAddon, error] {
	return paginate(params.Page, func(page, size int) ([]PlanAddon, int, error) {
		pageParams := params
		pageParams.Page = &PageParams{Size: Int(size), Number: Int(page)}

		resp, err := s.List(ctx, planID, pageParams)
		if err != nil {
			return nil, 0, err
		}

		return resp.Data, resp.Meta.Page.Total, nil
	})
}

// Add an add-on to a plan.
func (s *PlanAddonsService) Create(ctx context.Context, planID string, request CreatePlanAddonRequest) (*PlanAddon, error) {
	if planID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "planID", ErrEmptyID)
	}

	path := "/openmeter/plans/{planId}/addons"

	path = replacePathParam(path, "planId", planID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodPost, path, nil, request, "application/json", "application/json")
	if err != nil {
		return nil, err
	}

	var out PlanAddon
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// Get an add-on association for a plan.
func (s *PlanAddonsService) Get(ctx context.Context, planID string, planAddonID string) (*PlanAddon, error) {
	if planID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "planID", ErrEmptyID)
	}

	if planAddonID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "planAddonID", ErrEmptyID)
	}

	path := "/openmeter/plans/{planId}/addons/{planAddonId}"

	path = replacePathParam(path, "planId", planID)

	path = replacePathParam(path, "planAddonId", planAddonID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodGet, path, nil, nil, "", "application/json")
	if err != nil {
		return nil, err
	}

	var out PlanAddon
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// Update an add-on association for a plan.
func (s *PlanAddonsService) Update(ctx context.Context, planID string, planAddonID string, request UpsertPlanAddonRequest) (*PlanAddon, error) {
	if planID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "planID", ErrEmptyID)
	}

	if planAddonID == "" {
		return nil, fmt.Errorf("openmeter: %s must not be empty: %w", "planAddonID", ErrEmptyID)
	}

	path := "/openmeter/plans/{planId}/addons/{planAddonId}"

	path = replacePathParam(path, "planId", planID)

	path = replacePathParam(path, "planAddonId", planAddonID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodPut, path, nil, request, "application/json", "application/json")
	if err != nil {
		return nil, err
	}

	var out PlanAddon
	if err := s.client.doJSON(req, &out); err != nil {
		return nil, err
	}

	return &out, nil
}

// Remove an add-on from a plan.
func (s *PlanAddonsService) Delete(ctx context.Context, planID string, planAddonID string) error {
	if planID == "" {
		return fmt.Errorf("openmeter: %s must not be empty: %w", "planID", ErrEmptyID)
	}

	if planAddonID == "" {
		return fmt.Errorf("openmeter: %s must not be empty: %w", "planAddonID", ErrEmptyID)
	}

	path := "/openmeter/plans/{planId}/addons/{planAddonId}"

	path = replacePathParam(path, "planId", planID)

	path = replacePathParam(path, "planAddonId", planAddonID)

	req, err := s.client.newRequestWithContentType(ctx, http.MethodDelete, path, nil, nil, "", "")
	if err != nil {
		return err
	}

	_, err = s.client.doRaw(req)
	return err
}
