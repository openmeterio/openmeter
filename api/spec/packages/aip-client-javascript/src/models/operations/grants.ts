// Code generated by @openmeter/typespec-typescript. DO NOT EDIT.

import { z } from 'zod'
import * as schemas from '../schemas.js'
import type { AcceptDateStrings } from '../../lib/wire.js'
import type {
  CursorPaginationQueryPage,
  EntitlementGrantPaginatedResponse,
  ListGrantsParamsFilter,
  SortQueryInput,
} from '../types.js'

export interface ListGrantsQuery {
  page?: CursorPaginationQueryPage
  /**
   * Sort grants returned in the response. Supported sort attributes are:
   *
   * - `created_at` (default)
   * - `effective_at`
   *
   * The `asc` suffix is optional as the default sort order is ascending. The `desc`
   * suffix is used to specify a descending order.
   */
  sort?: SortQueryInput
  /**
   * Filter grants returned in the response.
   *
   * To filter grants by customer, add the following query param:
   * `filter[customer_id]=01K4WAQ0J99ZZ0MD75HXR112H8`
   */
  filter?: ListGrantsParamsFilter
  /** Include deleted grants in the response. */
  includeDeleted?: boolean
}

export type ListGrantsRequest = AcceptDateStrings<ListGrantsQuery>
export type ListGrantsResponse = EntitlementGrantPaginatedResponse

export interface VoidGrantQuery {
  /**
   * The time the grant is voided from. Defaults to now; it cannot be in the future
   * or before the start of the current usage period.
   */
  voidedAt?: Date
}

export type VoidGrantRequest = AcceptDateStrings<
  VoidGrantQuery & { grantId: string }
>
export type VoidGrantResponse = void
