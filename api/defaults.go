package api

const (
	// DefaultCreditQueryLimit specifies how many entries to return by default for credit related queries
	DefaultCreditQueryLimit = 1000
	// MaxCreditQueryLimit specifies how many entries we are returning max
	MaxCreditQueryLimit = DefaultCreditQueryLimit
)
