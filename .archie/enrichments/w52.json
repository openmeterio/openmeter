{
  "openmeter/apiconverter": {
    "purpose": "Thin translation layer converting v1 API filter/pagination types (api.Filter*) into internal pkg/filter and pkg/pagination/v2 types. All converter functions are Goverter-generated package-level vars; manual code is limited to cursor decoding and the goverter directive file.",
    "patterns": [
      {
        "name": "Goverter variable declarations in filter.go",
        "description": "Converter functions are package-level var of function type with goverter directive comments. Never implement conversion by hand \u2014 add a new var with goverter:ignore annotations and let go generate produce filter.gen.go.",
        "example": "// goverter:ignore Exists Contains Ncontains\nvar ConvertString func(api.FilterString) filter.FilterString"
      },
      {
        "name": "goverter:ignore for API-internal type mismatches",
        "description": "When the internal filter type has fields not present in the v1 API type (Exists, Contains, Ncontains), add a // goverter:ignore <Field> directive above the var. Remove when the v1 API grows the field.",
        "example": "// goverter:ignore Eq\n// goverter:ignore Exists\nConvertTime func(api.FilterTime) filter.FilterTime"
      },
      {
        "name": "Nil-safe pointer variants for every converter",
        "description": "Every converter has a *Ptr variant (ConvertStringPtr) handling nil input, so callers never need inline nil-checks.",
        "example": "ConvertStringPtr = func(source *api.FilterString) *filter.FilterString { if source != nil { v := ConvertString(*source); return &v }; return nil }"
      },
      {
        "name": "Cursor conversion delegates to pagination.DecodeCursor",
        "description": "ConvertCursor and ConvertCursorPtr in cursor.go are hand-written thin wrappers over pagination.DecodeCursor; keep cursor logic here, not in httpdriver packages.",
        "example": "func ConvertCursor(s api.CursorPaginationCursor) (*pagination.Cursor, error) { return pagination.DecodeCursor(s) }"
      },
      {
        "name": "init() block initializes all generated vars",
        "description": "filter.gen.go uses a single init() to assign all converter vars. The generated file carries the goverter DO NOT EDIT header.",
        "example": "func init() { ConvertBoolean = func(source api.FilterBoolean) filter.FilterBoolean { ... } }"
      }
    ],
    "key_file_guides": [
      {
        "file": "filter.go",
        "role": "Source of truth for all converter declarations; only var declarations and goverter directives. Run go generate ./ here to regenerate filter.gen.go.",
        "watch_for": "Adding hand-written conversion logic here \u2014 all logic must live in the generated filter.gen.go."
      },
      {
        "file": "filter.gen.go",
        "role": "Goverter-generated converter implementations.",
        "watch_for": "DO NOT EDIT \u2014 manual edits are overwritten on next go generate."
      },
      {
        "file": "cursor.go",
        "role": "Hand-written cursor conversion using pagination.DecodeCursor from pkg/pagination/v2.",
        "watch_for": "Duplicating cursor decoding elsewhere in httpdriver packages instead of calling ConvertCursor/ConvertCursorPtr."
      }
    ],
    "anti_patterns": [
      "Hand-writing conversion logic in filter.go instead of declaring a goverter var and regenerating",
      "Editing filter.gen.go directly \u2014 it is fully generated and will be overwritten",
      "Adding domain business logic here; this package is a pure type-translation boundary",
      "Forgetting goverter:ignore for fields that exist on internal types but not on the v1 API type",
      "Adding v3 API types here \u2014 v3 has its own converter layer"
    ],
    "decisions": [
      {
        "decision": "Use Goverter variable-style converters instead of methods or standalone functions",
        "rationale": "Variable-style converters allow recursive self-references (ConvertString calling itself for And/Or slices) and initialize in one init() block, keeping the generated code self-contained."
      },
      {
        "decision": "Keep cursor conversion hand-written rather than Goverter-generated",
        "rationale": "pagination.DecodeCursor performs base64 decoding that cannot be mechanically derived by field mapping, so it must stay hand-written in cursor.go."
      }
    ],
    "code_examples": [
      {
        "scenario": "Adding a new filter type converter for a hypothetical FilterEnum API type",
        "code": "// In filter.go \u2014 add a new var with directives:\n// goverter:ignoreMissing\nvar ConvertEnum    func(api.FilterEnum) filter.FilterEnum\nvar ConvertEnumPtr func(*api.FilterEnum) *filter.FilterEnum\n\n// Then run: go generate ./\n// This regenerates filter.gen.go with the new ConvertEnum and ConvertEnumPtr implementations."
      }
    ]
  },
  "openmeter/session": {
    "purpose": "Context-scoped authentication session carrier for the cloud/managed-hosting auth layer. Stores AuthenticationSession (org ID, role, permissions, user ID) in context under a typed key; used by auth middleware to propagate identity through request handlers.",
    "patterns": [
      {
        "name": "Typed context key to avoid collisions",
        "description": "AuthenticatorContextKey is a named string type; AuthenticationSessionKey is of that type. Never use a plain string as a context key.",
        "example": "type AuthenticatorContextKey string\nconst AuthenticationSessionKey AuthenticatorContextKey = \"active_organization_id\""
      },
      {
        "name": "GetActiveSession returns nil on missing or wrong type",
        "description": "Uses a type assertion with ok-check and returns nil instead of panicking. Callers must nil-check before using the session.",
        "example": "func GetActiveSession(ctx context.Context) *AuthenticationSession { if c, ok := ctx.Value(AuthenticationSessionKey).(*AuthenticationSession); ok { return c }; return nil }"
      },
      {
        "name": "Validate on construction",
        "description": "NewAuthenticationSession calls session.Validate() before returning; invalid sessions are rejected at construction. OrgRole OR OrgPermissions must be non-empty.",
        "example": "session, err := NewAuthenticationSession(orgID, orgSlug, orgRole, userID, perms) // err if OrgID empty or both OrgRole and OrgPermissions empty"
      },
      {
        "name": "WithLogger enriches slog with session fields",
        "description": "AuthenticationSession.WithLogger(logger) adds orgId, userId, orgSlug, orgRole, orgPermissions as structured log fields.",
        "example": "logger = session.WithLogger(logger)"
      }
    ],
    "key_file_guides": [
      {
        "file": "session.go",
        "role": "Entire package: context key type, AuthenticationSession struct, constructor, validator, context getter helpers, logger enrichment.",
        "watch_for": "Storing AuthenticationSession by value in context \u2014 the getter type-asserts to *AuthenticationSession, so a value type always returns nil from GetActiveSession."
      }
    ],
    "anti_patterns": [
      "Storing AuthenticationSession by value in context (must be pointer *AuthenticationSession)",
      "Using a plain string context key instead of the typed AuthenticatorContextKey constant",
      "Calling ctx.Value(AuthenticationSessionKey) directly outside of GetActiveSession \u2014 always use the helper",
      "Adding business logic (authorization checks, permission resolution) to this package \u2014 it is purely a session carrier"
    ],
    "decisions": [
      {
        "decision": "OrgRole OR OrgPermissions must be non-empty, not both required",
        "rationale": "Different auth flows supply either a role string or a fine-grained permission list; requiring only one keeps the session compatible with both flows without two separate session types."
      }
    ],
    "code_examples": []
  },
  "openmeter/testutils": {
    "purpose": "Shared, app/common-independent test helpers for the entire openmeter/ tree: Postgres test database provisioning via pgtestdb, async assertion helpers, deterministic name generation, logger factories, and time utilities. Must never import app/common to avoid import cycles.",
    "patterns": [
      {
        "name": "InitPostgresDB skips when POSTGRES_HOST unset",
        "description": "InitPostgresDB reads os.Getenv(\"POSTGRES_HOST\") and calls t.Skip() if empty. Tests are silently skipped in environments without Postgres rather than failing.",
        "example": "db := testutils.InitPostgresDB(t); t.Cleanup(func() { db.Close(t) })"
      },
      {
        "name": "TestDB bundles EntDriver + PGDriver + URL",
        "description": "InitPostgresDB returns a *TestDB with both the Ent driver and raw pgdriver.Driver. Call db.Close(t) in t.Cleanup to release both.",
        "example": "db := testutils.InitPostgresDB(t); t.Cleanup(func() { db.Close(t) }); client := db.EntDriver.Client()"
      },
      {
        "name": "NoopMigrator default; WithMigrator for schema-applying tests",
        "description": "Default migrator is NoopMigrator. Pass WithMigrator(myMigrator) to apply real schema migrations; otherwise pgtestdb clones a blank template.",
        "example": "db := testutils.InitPostgresDB(t, testutils.WithMigrator(&myMigrator{}))"
      },
      {
        "name": "t.Context() throughout \u2014 no context.Background()",
        "description": "All helpers use t.Context() so test cancellation propagates. Never substitute context.Background() in new test helpers.",
        "example": "postgresDriver, err := pgdriver.NewPostgresDriver(t.Context(), dbConf.URL(), o.driverOptions...)"
      },
      {
        "name": "EventuallyWithTf wraps require.EventuallyWithTf with error capture",
        "description": "Use EventuallyWithTf when the polling function must report its last error on final failure. Pass a saveErr closure to record the error.",
        "example": "testutils.EventuallyWithTf(t, func(c *assert.CollectT, saveErr func(err any)) { saveErr(doCheck(c)) }, 5*time.Second, 100*time.Millisecond)"
      },
      {
        "name": "NameGenerator produces Key + Name pairs",
        "description": "NameGenerator.Generate() returns a GeneratedName with a slug Key (lowercase, hyphen-separated) and a Title-case Name. Use Key for DB identifiers, Name for display.",
        "example": "n := testutils.NameGenerator.Generate() // n.Key = \"happy-tiger\", n.Name = \"Happy Tiger\""
      }
    ],
    "key_file_guides": [
      {
        "file": "pg_driver.go",
        "role": "Core Postgres test provisioning: InitPostgresDB, TestDB, option types (WithPostgresConfig, WithMigrator, WithDriverOptions). Most-imported file.",
        "watch_for": "Importing app/common here creates an import cycle. Build Ent clients directly from entdriver.NewEntPostgresDriver(postgresDriver.DB())."
      },
      {
        "file": "namegen.go",
        "role": "Deterministic (per-run) human-readable name generator seeded at package init using forscht/namegen.",
        "watch_for": "Calling NameGenerator.Generate() concurrently from multiple goroutines without locking \u2014 the underlying generator is not goroutine-safe."
      },
      {
        "file": "async.go",
        "role": "EventuallyWithTf helper for async assertions needing last-error capture via sync.Map.",
        "watch_for": "Using plain require.Eventually when the polling body produces a meaningful error \u2014 EventuallyWithTf surfaces it on failure."
      },
      {
        "file": "logger.go",
        "role": "NewLogger (slog.Default) and NewDiscardLogger (inline discardHandler) factories for test logging.",
        "watch_for": "Using slog.New(slog.NewTextHandler(os.Stdout, nil)) inline in tests \u2014 use NewLogger(t) for consistency."
      },
      {
        "file": "time.go",
        "role": "GetRFC3339Time (t.Fatalf on parse error) and TimeEqualsApproximately for tolerant time comparison.",
        "watch_for": "Using time.Parse directly in tests \u2014 GetRFC3339Time wraps the error into t.Fatalf, keeping test code clean."
      }
    ],
    "anti_patterns": [
      "Importing app/common or any Wire provider from this package \u2014 breaks test-helper isolation and creates import cycles",
      "Using context.Background() in helper functions \u2014 always use t.Context()",
      "Skipping db.Close(t) in test teardown \u2014 leaks both Ent and PG connections",
      "Implementing domain-specific test logic here \u2014 domain fixtures belong in openmeter/<domain>/testutils/",
      "Editing pg_driver.go to apply schema migrations by default \u2014 use the WithMigrator option"
    ],
    "decisions": [
      {
        "decision": "NoopMigrator as default migrator in InitPostgresDB",
        "rationale": "Most test suites manage their own schema (Ent auto-migrate or explicit DDL); a no-op default avoids coupling this shared helper to the Atlas migration chain or any domain schema."
      },
      {
        "decision": "TestDB bundles both EntDriver and PGDriver",
        "rationale": "Some tests need the Ent typed client; others need the raw pgx pool (e.g. direct SQL assertions). Providing both avoids callers reconstructing drivers from the URL string."
      }
    ],
    "code_examples": [
      {
        "scenario": "Standard Postgres-backed unit test setup",
        "code": "import (\n\t\"testing\"\n\t\"github.com/openmeterio/openmeter/openmeter/testutils\"\n)\n\nfunc TestSomething(t *testing.T) {\n\tdb := testutils.InitPostgresDB(t) // skips if POSTGRES_HOST unset\n\tt.Cleanup(func() { db.Close(t) })\n\n\tentClient := db.EntDriver.Client()\n\t// use entClient for domain adapter construction\n}"
      }
    ]
  }
}