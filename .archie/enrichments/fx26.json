{
  "api/v3/test": {
    "purpose": "Integration test fixtures and tests for the v3 API filter parsing and OAS validation pipeline. Provides an embedded minimal OpenAPI spec (openapi.test.yaml) and end-to-end tests that spin up a real Chi+oasmiddleware server to verify every filter type is correctly validated then parsed by filters.Parse.",
    "patterns": [
      {
        "name": "Embedded test spec via //go:embed",
        "description": "openapi.test.yaml is embedded as []byte via embed.go (OpenAPITestSpec) and loaded through openapi3.NewLoader().LoadFromData. Tests never read the file from disk at runtime.",
        "example": "//go:embed openapi.test.yaml\nvar OpenAPITestSpec []byte"
      },
      {
        "name": "newTestServer bootstraps the full validation stack",
        "description": "Test helpers build a real Chi router with oasmiddleware.ValidateRequest and httptest.NewServer so tests exercise OAS schema validation before reaching the handler.",
        "example": "srv := newTestServer(t, noopHandler) // Chi + oasmiddleware over the embedded spec"
      },
      {
        "name": "Two-phase test structure: validation then parse",
        "description": "Validation tests assert OAS acceptance/rejection (204/400) with noopHandler; parse tests additionally run filters.Parse and assert the resulting typed Go struct with parseHandler. Both must pass for a valid filter.",
        "example": "// TestFieldFilterValidation -> 204/400; TestFieldFilterParse -> typed struct"
      },
      {
        "name": "t.Context() for all test contexts",
        "description": "Every context value uses t.Context() tied to the test lifecycle, never context.Background().",
        "example": "validationRouter, err := oasmiddleware.NewValidationRouter(t.Context(), doc, ...)"
      }
    ],
    "key_file_guides": [
      {
        "file": "embed.go",
        "role": "Embeds openapi.test.yaml as OpenAPITestSpec []byte. Package name is 'test'.",
        "watch_for": "filters_test.go uses package 'test_test' to test as an external consumer; embed.go itself stays in package 'test'."
      },
      {
        "file": "openapi.test.yaml",
        "role": "Minimal OpenAPI 3.0 spec defining the FieldFilters schema with every supported filter type (BooleanFieldFilter, NumericFieldFilter, StringFieldFilter, etc.) mapped via x-go-type to the api/v3/filters package.",
        "watch_for": "x-go-type-import/x-go-type annotations drive oapi-codegen output; a new filter type here needs a matching type in api/v3/filters."
      },
      {
        "file": "filters_test.go",
        "role": "End-to-end filter validation and parse tests covering all filter types and operators in both short form (filter[field]=value) and object form (filter[field][op]=value).",
        "watch_for": "ULID/DateTime pattern/format violations surface as anyOf-collapse errors (rule: 'anyOf'), not per-branch errors, due to kin-openapi behaviour; a bare scalar (non-map) Labels value is rejected at the validator level."
      }
    ],
    "anti_patterns": [
      "Reading openapi.test.yaml from the filesystem at test time instead of using the embedded OpenAPITestSpec",
      "Using context.Background() in tests instead of t.Context()",
      "Adding production API endpoints to openapi.test.yaml \u2014 it is a minimal fixture for filter type testing only"
    ],
    "decisions": [
      {
        "decision": "Separate minimal test spec (openapi.test.yaml) rather than reusing the production spec",
        "rationale": "Isolates filter-type validation from the full API surface, making the spec faster to load and the tests focused on filter behaviour only."
      }
    ],
    "code_examples": []
  },
  "app/common": {
    "purpose": "Houses all Google Wire provider sets and constructor functions that assemble domain services, Kafka/DB clients, telemetry, and per-binary run groups \u2014 zero business logic. One file per domain concern; openmeter_<binary>.go files define the composite Wire set per binary.",
    "patterns": [
      {
        "name": "One domain file per Wire set",
        "description": "Each domain concern has exactly one file (billing.go, customer.go, ledger.go, ...) exporting a PascalCase wire.NewSet var. Binary-specific composite sets live in openmeter_<binary>.go and compose the domain sets.",
        "example": "var Billing = wire.NewSet(BillingAdapter, NewBillingRatingService, NewBillingRegistry, NewBillingCustomerOverrideService)"
      },
      {
        "name": "Registry structs for multi-service domains",
        "description": "Group related services in a <Domain>Registry (BillingRegistry, AppRegistry, ChargesRegistry); callers depend on the registry. Nil-safe accessors encapsulate optional sub-registries.",
        "example": "type BillingRegistry struct { Billing billing.Service; Charges *ChargesRegistry }; func (r BillingRegistry) ChargesServiceOrNil() charges.Service { if r.Charges == nil { return nil }; return r.Charges.Service }"
      },
      {
        "name": "creditsConfig.Enabled guard in every ledger-touching provider",
        "description": "Every provider that can trigger ledger writes independently checks creditsConfig.Enabled and returns a noop when false. Four independent layers: ledger.go, customer.go, customerbalance.go, billing.go (newChargesRegistry skipped). A single guard is insufficient.",
        "example": "func NewLedgerAccountService(creditsConfig config.CreditsConfiguration, ...) ledgeraccount.Service { if !creditsConfig.Enabled { return ledgernoop.AccountService{} }; return accountservice.New(...) }"
      },
      {
        "name": "Hook/validator registration as provider side-effects",
        "description": "Cross-domain hooks (billing customer validator, subscription hook, ledger hook, entitlement validator) are registered inside app/common provider functions \u2014 never inside domain constructors \u2014 to avoid circular imports.",
        "example": "customerService.RegisterRequestValidator(validator); subscriptionServices.Service.RegisterHook(subscriptionValidator) // inside NewBillingRegistry"
      },
      {
        "name": "Noop (never nil) for optional integrations",
        "description": "Optional integrations (Svix, credits, portal) return compile-time-asserting noop structs when disabled. Type-assert against the noop type to conditionally skip handler registration.",
        "example": "func NewLedgerNamespaceHandler(ar ledger.AccountResolver) namespace.Handler { if _, ok := ar.(ledgernoop.AccountResolver); ok { return ledgernoop.NamespaceHandler{} }; return resolvers.NewNamespaceHandler(ar) }"
      },
      {
        "name": "Closer function pattern for stateful resources",
        "description": "Providers returning closeable resources (postgres driver, ClickHouse conn, Kafka consumer) return a func() closer as a second value; Wire propagates it to the binary's run.Group / shutdown.",
        "example": "func NewPostgresDriver(...) (*pgdriver.Driver, func(), error) { d, err := pgdriver.NewPostgresDriver(...); return d, func() { d.Close() }, nil }"
      },
      {
        "name": "config.go wire.FieldsOf for sub-struct injection",
        "description": "config.go declares all wire.FieldsOf bindings decomposing config.Configuration into injectable sub-structs. A new injectable config sub-struct needs a FieldsOf entry here first.",
        "example": "wire.FieldsOf(new(config.Configuration), \"Credits\"), wire.FieldsOf(new(config.BillingConfiguration), \"FeatureSwitches\")"
      }
    ],
    "key_file_guides": [
      {
        "file": "billing.go",
        "role": "Wires the full billing + charges stack; private newChargesRegistry runs only when creditsConfig.Enabled. Registers billing customer and subscription validators as side-effects inside NewBillingRegistry.",
        "watch_for": "ChargesRegistry is nil when credits disabled \u2014 use BillingRegistry.ChargesServiceOrNil(), never access .Charges directly. Any new ledger write path must be gated on creditsConfig.Enabled."
      },
      {
        "file": "charges.go",
        "role": "Constructors for every charges sub-service (flatfee, usagebased, creditpurchase, lineage, meta) plus their ledger handlers. Registers line engines via billingService.RegisterLineEngine as a side-effect.",
        "watch_for": "Each charge type engine must be registered before the first invoice advance. Called only from newChargesRegistry in billing.go \u2014 never instantiate directly from a binary."
      },
      {
        "file": "ledger.go",
        "role": "Full LedgerStack provider set. Every function independently checks creditsConfig.Enabled and returns noop types when false; NewLedgerNamespaceHandler skips the real handler via type-assert against ledgernoop.AccountResolver.",
        "watch_for": "NewLedgerHistoricalLedger intentionally creates a second accountservice.New with no Querier to avoid circular deps \u2014 keep the comment. Every new ledger provider must add its own Enabled guard."
      },
      {
        "file": "customer.go",
        "role": "Creates customer.Service and registers the ledger hook (creditsConfig.Enabled-guarded), subject hook, and entitlement validator hook as provider side-effects.",
        "watch_for": "NewCustomerLedgerServiceHook must return ledgerresolvers.NoopCustomerLedgerHook{} when credits disabled \u2014 this is independent of ledger.go; both must be guarded."
      },
      {
        "file": "config.go",
        "role": "Declares all wire.FieldsOf bindings decomposing config.Configuration into injectable sub-structs. Single source of truth for which config paths Wire knows about.",
        "watch_for": "A new injectable config sub-struct needs a wire.FieldsOf entry here; a missing entry causes Wire compile failures in binary-specific graphs."
      },
      {
        "file": "openmeter_billingworker.go",
        "role": "BillingWorker composite set for cmd/billing-worker, composing App, Customer, Secret, Lockr, Subscription, ProductCatalog, Entitlement, Billing, LedgerStack and wiring the Watermill subscriber and run.Group.",
        "watch_for": "BillingWorkerGroup adds run.SignalHandler \u2014 do not add another. EnsureBusinessAccounts must run in the startup sequence before app.Run()."
      },
      {
        "file": "watermill.go",
        "role": "Watermill + Kafka publisher/subscriber wiring. WatermillNoPublisher set is shared with sink-worker, which controls publisher close timing independently.",
        "watch_for": "Sink-worker uses NewSinkWorkerPublisher (in openmeter_sinkworker.go) returning an empty closer \u2014 do not add publisher.Close() in FlushHandlerManager until drain is complete."
      },
      {
        "file": "telemetry.go",
        "role": "Bootstraps OTel logger/meter/tracer providers, Prometheus handler, health checks, and the telemetry HTTP server. GlobalInitializer.SetGlobals() must be called early in main.",
        "watch_for": "Shutdown contexts use context.Background() with a 5s timeout intentionally (post-cancel graceful shutdown), not a bug."
      }
    ],
    "anti_patterns": [
      "Adding business logic (validation, computation, state mutation) inside provider functions \u2014 they may only construct/wire plus side-effect hook registration",
      "Calling customerService.RegisterHooks / RegisterRequestValidator from domain packages instead of here, reintroducing circular imports",
      "Creating a new ledger-backed provider without a creditsConfig.Enabled guard returning a noop",
      "Accessing BillingRegistry.Charges directly without nil-check instead of ChargesServiceOrNil()",
      "Importing app/common from any openmeter/* or pkg/* package \u2014 this is a leaf that depends on everything else, never the reverse"
    ],
    "decisions": [
      {
        "decision": "Concentrate all wiring in app/common while domain packages expose plain constructors",
        "rationale": "Compile-time Wire verification catches missing providers; seven binaries share provider sets without duplicating wiring; domain packages stay import-cycle-free because wiring flows outward only."
      },
      {
        "decision": "Guard the credits feature independently in each ledger-touching provider rather than at one choke point",
        "rationale": "Credits cross-cuts ledger writes, customer hooks, namespace provisioning, and HTTP handlers across unrelated call graphs \u2014 no single injection point dominates all paths, so each must independently return noops."
      },
      {
        "decision": "Registry structs (BillingRegistry, AppRegistry, SubscriptionServiceWithWorkflow) instead of injecting individual services",
        "rationale": "Groups cohesive services, reduces Wire graph complexity for callers, and lets ChargesServiceOrNil() encapsulate the credits-disabled nil case without scattered checks."
      }
    ],
    "code_examples": [
      {
        "scenario": "Add a new ledger-backed provider that must be disabled when credits are off",
        "code": "// app/common/newdomain.go\nfunc NewMyDomainService(\n    creditsConfig config.CreditsConfiguration,\n    ledgerService ledger.Ledger,\n    db *entdb.Client,\n) (mydomain.Service, error) {\n    if !creditsConfig.Enabled {\n        return mydomainnoop.NewService(), nil\n    }\n    adapter, err := mydomainadapter.New(mydomainadapter.Config{Client: db})\n    if err != nil {\n        return nil, fmt.Errorf(\"init mydomain adapter: %w\", err)\n    }\n    return mydomainservice.New(mydomainservice.Config{Adapter: adapter})\n}"
      }
    ]
  },
  "app/config": {
    "purpose": "Defines the single shared Viper-based config.Configuration struct used by all seven binaries, one file per concern. Each file pairs a typed struct, a Validate() that accumulates errors via errors.Join, and a Configure*(v) that sets Viper defaults / pflag bindings.",
    "patterns": [
      {
        "name": "One struct + Validate() + Configure*() per concern file",
        "description": "Each concern has a typed struct, a Validate() error accumulating via errors.Join, and a Configure*(v *viper.Viper) setting defaults. No global state, no side-effects beyond viper defaults.",
        "example": "type BillingConfiguration struct { AdvancementStrategy billing.AdvancementStrategy; Worker BillingWorkerConfiguration }; func (c BillingConfiguration) Validate() error { var errs []error; /* ... */ return errors.Join(errs...) }"
      },
      {
        "name": "SetViperDefaults as the single registration point",
        "description": "config.go SetViperDefaults calls every Configure* function in order; a new concern must add its Configure* call here before it is loaded. ConfigureCredits is currently called twice (known duplication).",
        "example": "func SetViperDefaults(v *viper.Viper, flags *pflag.FlagSet) { ConfigureBilling(v, flags); ConfigureProductCatalog(v); ConfigureApps(v, flags); /* ... */ ConfigureCredits(v, \"credits\") }"
      },
      {
        "name": "Helper methods for derived client types",
        "description": "Config structs expose helpers (AsURL(), GetClientOptions(), AsConsumerConfig(), AsConfigMap()) that translate raw fields into third-party client types, keeping translation out of app/common.",
        "example": "func (c ClickHouseAggregationConfiguration) GetClientOptions() *clickhouse.Options { return &clickhouse.Options{Addr: []string{c.Address}, Auth: clickhouse.Auth{Database: c.Database, Username: c.Username, Password: c.Password}, ...} }"
      },
      {
        "name": "Validate() accumulates all errors with errors.Join",
        "description": "Validate() methods build var errs []error, append each failure (sub-struct errors prefixed with errorsx.WithPrefix or fmt.Errorf), and return errors.Join(errs...) so all misconfigs surface at once.",
        "example": "if err := c.ConsumerConfiguration.Validate(); err != nil { errs = append(errs, errorsx.WithPrefix(err, \"consumer\")) }"
      },
      {
        "name": "Consumer config squash embedding for Kafka worker configs",
        "description": "Worker configs embed ConsumerConfiguration with mapstructure:\",squash\" to inherit Kafka consumer settings without field duplication.",
        "example": "type BalanceWorkerConfiguration struct { ConsumerConfiguration `mapstructure:\",squash\"`; StateStorage BalanceWorkerStateStorageConfiguration }"
      }
    ],
    "key_file_guides": [
      {
        "file": "config.go",
        "role": "Root Configuration struct aggregating all sub-configs; SetViperDefaults registers all defaults; Validate() fans out to every sub-validator and validates meter definitions (setting ManagedResource per meter).",
        "watch_for": "Credits is validated twice (prefixes 'credits' and 'credit') and ConfigureCredits(v, \"credits\") is called twice in SetViperDefaults \u2014 known duplication. New sub-configs must be added to the struct, Validate(), and a Configure* call."
      },
      {
        "file": "billing.go",
        "role": "BillingConfiguration + BillingFeatureSwitchesConfiguration; FeatureSwitches.NamespaceLockdown is a []string allowlist gating billing operations per namespace.",
        "watch_for": "AdvancementStrategy references the openmeter/billing domain type directly \u2014 this is the intentional exception where a config file imports a domain package."
      },
      {
        "file": "aggregation.go",
        "role": "ClickHouse connection config (TLS, retry, pool metrics); GetClientOptions() builds *clickhouse.Options for the ClickHouse client.",
        "watch_for": "Numeric fields (dialTimeout, maxOpenConns, maxIdleConns, connMaxLifetime, blockBufferSize) must be > 0 \u2014 Validate() rejects zero values, so always set positive defaults in ConfigureAggregation or zero-value configs fail."
      },
      {
        "file": "kafka.go",
        "role": "KafkaConfiguration / KafkaIngestConfiguration with AsConfigMap()/CreateKafkaConfig() helpers; the ConsumerConfig produced here is consumed by app/common/kafka.go.",
        "watch_for": "KafkaIngestConfiguration.TopicProvisioner is decomposed via wire.FieldsOf in app/common/config.go \u2014 renaming a field requires updating the FieldsOf binding."
      },
      {
        "file": "credits.go",
        "role": "CreditsConfiguration with Enabled and EnableCreditThenInvoice flags; injected into every provider that must be credits-guarded.",
        "watch_for": "This is the type-safe flag checked in four independent wiring layers \u2014 do not add default-true fields without updating all ledger-guarded providers in app/common."
      }
    ],
    "anti_patterns": [
      "Adding business logic or mutable state to config structs \u2014 they are pure data containers with validation only",
      "Calling viper.SetDefault directly from cmd/* binaries instead of adding a Configure* function here",
      "Adding a config field without a corresponding Validate() check and Configure* default, producing silent zero-value misconfiguration",
      "Importing openmeter/* domain packages beyond openmeter/meter and openmeter/billing \u2014 keep domain imports minimal"
    ],
    "decisions": [
      {
        "decision": "Single shared config.Configuration type for all binaries",
        "rationale": "wire.FieldsOf in app/common decomposes it into typed sub-structs per domain so each binary injects only what it needs, while all binaries load the same config shape \u2014 preventing config drift across binaries."
      },
      {
        "decision": "Configure* functions set Viper defaults instead of init()/package-level vars",
        "rationale": "Explicit calls in SetViperDefaults make registration order visible, allow per-test selective invocation, and avoid global-state races between parallel test processes."
      },
      {
        "decision": "errors.Join accumulation in Validate() rather than fail-fast",
        "rationale": "Surfacing all config errors at startup is more actionable for operators, especially when one env-var change produces several invalid fields."
      }
    ],
    "code_examples": [
      {
        "scenario": "Add a new config concern for a hypothetical domain",
        "code": "// app/config/newdomain.go\ntype NewDomainConfiguration struct {\n    Enabled  bool\n    Endpoint string\n}\n\nfunc (c NewDomainConfiguration) Validate() error {\n    var errs []error\n    if c.Enabled && c.Endpoint == \"\" {\n        errs = append(errs, errors.New(\"endpoint is required when enabled\"))\n    }\n    return errors.Join(errs...)\n}\n\nfunc ConfigureNewDomain(v *viper.Viper) {\n    v.SetDefault(\"newDomain.enabled\", false)\n}"
      }
    ]
  }
}