{
  "api/v3/request": {
    "purpose": "HTTP request parsing and query parameter conversion utilities for the v3 API layer. Translates raw HTTP inputs (JSON bodies, sort strings, API filter types) into internal domain types before they reach handlers.",
    "patterns": [
      {
        "name": "Return *apierrors.BaseAPIError not error",
        "description": "All parse functions return *apierrors.BaseAPIError (nilable pointer), not the Go error interface. Callers check for nil to detect parse failure.",
        "example": "func ParseBody(r *http.Request, payload any) *apierrors.BaseAPIError"
      },
      {
        "name": "Contains operator wraps to SQL LIKE pattern",
        "description": "api.QueryFilterString.Contains/Ncontains are converted to LIKE/NLIKE via filter.ContainsPattern, which escapes SQL metacharacters (%, _, \\). Never pass Contains values directly to filter.FilterString.Like.",
        "example": "Like: convertContainsOperator(source.Contains)"
      },
      {
        "name": "Recursive filter conversion",
        "description": "And/Or slices in QueryFilterString are converted recursively via convertQueryFilterStringList so nested boolean expressions are fully translated.",
        "example": "And: convertQueryFilterStringList(source.And)"
      },
      {
        "name": "SortBy text format: 'field [asc|desc]'",
        "description": "ParseSortBy parses a string with UnmarshalText: one or two whitespace-separated tokens. First token is Field, second (optional) is Order; default order is asc. Anything else returns ErrSortByInvalid.",
        "example": "parts := strings.Fields(string(text)); s.Field = parts[0]"
      },
      {
        "name": "Nil-safe pointer converters",
        "description": "ConvertQueryFilterStringPtr returns nil when given nil, avoiding nil-dereference in optional filter fields.",
        "example": "func ConvertQueryFilterStringPtr(source *api.QueryFilterString) *filter.FilterString"
      }
    ],
    "key_file_guides": [
      {
        "file": "body.go",
        "role": "JSON body decoder; ParseOptionalBody silently ignores io.EOF (empty body) to allow PATCH semantics.",
        "watch_for": "ParseOptionalBody leaves payload unchanged on empty body \u2014 callers must treat unchanged payload as 'no update', not 'zero value'."
      },
      {
        "file": "filter.go",
        "role": "Converts api.QueryFilterString / QueryFilterStringMapItem to pkg/filter.FilterString. QueryFilterStringMapItem adds an Exists field not present in QueryFilterString.",
        "watch_for": "Neq maps to Ne (field name differs between API and internal type). Contains maps to Like with wrapping %; do not set Like directly from Contains."
      },
      {
        "file": "sort.go",
        "role": "SortBy value type with UnmarshalText; ToSortxOrder converts to pkg/sortx.Order.",
        "watch_for": "Validate() is called inside UnmarshalText \u2014 callers must not skip validation. ErrSortFieldRequired fires when Field is empty after parsing."
      }
    ],
    "anti_patterns": [
      "Returning the Go error interface instead of *apierrors.BaseAPIError from a parse function",
      "Setting filter.FilterString.Like directly from an API Contains value without calling filter.ContainsPattern",
      "Calling Validate() separately after ParseSortBy \u2014 it is already called inside UnmarshalText",
      "Adding business logic or domain service calls in this package \u2014 it is pure translation/parsing only"
    ],
    "decisions": [
      {
        "decision": "Parse functions return *apierrors.BaseAPIError instead of error",
        "rationale": "Allows handlers to directly propagate structured API errors with field path and source metadata without wrapping or type-asserting."
      },
      {
        "decision": "Contains wraps to SQL LIKE pattern inside this package",
        "rationale": "Centralises metacharacter escaping so handlers never need to know about SQL LIKE syntax; all filter construction uses filter.ContainsPattern consistently."
      }
    ],
    "code_examples": [
      {
        "scenario": "Handler converts API filter query param to internal FilterString",
        "code": "import (\n    api \"github.com/openmeterio/openmeter/api/v3\"\n    \"github.com/openmeterio/openmeter/api/v3/request\"\n    \"github.com/openmeterio/openmeter/pkg/filter\"\n)\n\nvar f *filter.FilterString\nif params.Filter != nil {\n    f = request.ConvertQueryFilterStringPtr(params.Filter)\n}"
      }
    ]
  },
  "api/v3/response": {
    "purpose": "Reusable response envelope types for the v3 API: cursor-based and page-based pagination wrappers. All list endpoints must use one of these two structs to ensure consistent JSON shape across the API.",
    "patterns": [
      {
        "name": "Generic pagination wrappers",
        "description": "Both CursorPaginationResponse[T] and PagePaginationResponse[T] are generic over item type T. Handlers call the constructor (NewCursorPaginationResponse / NewPagePaginationResponse) instead of building the struct manually.",
        "example": "return response.NewCursorPaginationResponse(items, pageSize)"
      },
      {
        "name": "Cursor derived from Item interface",
        "description": "NewCursorPaginationResponse requires T to implement pagination.Item (exposes Cursor() method). First/Last cursors are set from items[0] and items[len-1]; Next is only set when a full page was returned.",
        "example": "func NewCursorPaginationResponse[T pagination.Item](items []T, pageSize int) CursorPaginationResponse[T]"
      },
      {
        "name": "nullable.Nullable for next/previous links",
        "description": "CursorMetaPage.Next and Previous are nullable.Nullable[string] (not *string) so they serialize as explicit JSON null when absent rather than being omitted entirely.",
        "example": "Next: nullable.NewNullNullable[string]()"
      }
    ],
    "key_file_guides": [
      {
        "file": "cursorpagination.go",
        "role": "Cursor-based pagination envelope; T must implement pagination.Item from pkg/pagination/v2.",
        "watch_for": "Next cursor is only populated when len(items) == pageSize; a partial page signals last page. Do not set Next manually."
      },
      {
        "file": "pagepagination.go",
        "role": "Offset/page-number pagination envelope; PageMetaPage.Total is optional (*int) for cases where total count is expensive.",
        "watch_for": "Caller must populate PageMetaPage fully (Size, Number, Total) before passing to NewPagePaginationResponse \u2014 no defaults are inferred."
      }
    ],
    "anti_patterns": [
      "Returning a raw []T from a list endpoint instead of wrapping in CursorPaginationResponse or PagePaginationResponse",
      "Setting CursorMetaPage.Next to a non-null value when fewer items than pageSize were returned",
      "Using *string for Next/Previous fields \u2014 must use nullable.Nullable[string] to produce explicit JSON null"
    ],
    "decisions": [
      {
        "decision": "Cursor is encoded directly from the last item in the page rather than stored separately",
        "rationale": "Avoids out-of-sync cursor state; the item itself is the canonical source of its own cursor position."
      }
    ],
    "code_examples": []
  },
  "api/v3/server": {
    "purpose": "The v3 HTTP server: wires all domain service dependencies into typed handler structs, validates the Config, registers OpenAPI request validation middleware on a Chi router, and delegates every generated ServerInterface method to the appropriate handler. This is the sole assembly point for the v3 API surface.",
    "patterns": [
      {
        "name": "Config validation before NewServer",
        "description": "Config.Validate() is called at the top of NewServer and returns a wrapped error listing all missing required services. Never skip validation.",
        "example": "if err := config.Validate(); err != nil { return nil, fmt.Errorf(\"invalid v3 server config: %w\", err) }"
      },
      {
        "name": "credits.enabled gated at route level AND constructor level",
        "description": "Credits routes in routes.go check both s.Credits.Enabled and s.customersCreditsHandler != nil before dispatching. In NewServer, when credits are disabled, noop implementations replace real services. Both guards are required.",
        "example": "if !s.Credits.Enabled || s.customersCreditsHandler == nil { unimplemented.GetCustomerCreditBalance(w, r, ...); return }"
      },
      {
        "name": "Handler delegation via .With(params).ServeHTTP(w, r)",
        "description": "Each ServerInterface method calls the typed handler method, optionally passes path/query params via .With(...), and calls ServeHTTP. No business logic lives in routes.go.",
        "example": "s.metersHandler.GetMeter().With(meterId).ServeHTTP(w, r)"
      },
      {
        "name": "resolveNamespace closure injected into every handler",
        "description": "Namespace is not a URL path segment; a resolveNamespace func(ctx) (string, error) closure is constructed once in NewServer and injected into all handler constructors.",
        "example": "resolveNamespace := func(ctx context.Context) (string, error) { ns, ok := config.NamespaceDecoder.GetNamespace(ctx); ... }"
      },
      {
        "name": "Optional handlers guarded by nil checks",
        "description": "Handlers for optional services (ChargeService, LLMCostService, CostService) are only constructed when the service is non-nil; the server field stays nil otherwise. Routes must nil-check or delegate to api.Unimplemented{}.",
        "example": "if config.ChargeService != nil { chargesH = chargeshandler.New(...) }"
      },
      {
        "name": "Content negotiation at route level",
        "description": "QueryMeter inspects Accept header via commonhttp.GetMediaType; text/csv routes to the CSV handler, all other types use JSON handler. Content negotiation is done in routes.go, not in the handler.",
        "example": "if mediaType, _ := commonhttp.GetMediaType(r); mediaType == \"text/csv\" { s.metersHandler.QueryMeterCSV()... }"
      },
      {
        "name": "var _ api.ServerInterface = (*Server)(nil) compile-time check",
        "description": "A blank-identifier compile-time assertion ensures Server implements the generated ServerInterface. Must be kept; removing it hides missing route implementations.",
        "example": "var _ api.ServerInterface = (*Server)(nil)"
      }
    ],
    "key_file_guides": [
      {
        "file": "server.go",
        "role": "Config struct definition, Config.Validate, Server struct with all handler fields, NewServer constructor, RegisterRoutes with OAS validation middleware.",
        "watch_for": "Adding a new domain service requires: field in Config, nil-check in Validate (if required), handler construction in NewServer, handler field in Server struct. Missing any step causes a panic or silent noop."
      },
      {
        "file": "routes.go",
        "role": "One method per generated ServerInterface operation; pure delegation to handler. Contains all credits feature-flag guards.",
        "watch_for": "Credits endpoints must check both s.Credits.Enabled and handler != nil. Multi-param routes (e.g. ListCostBases) pack args into a local struct (e.g. currencieshandler.ListCostBasesArgs) before .With(...)."
      }
    ],
    "anti_patterns": [
      "Adding business logic or error handling inside routes.go \u2014 it must be pure delegation",
      "Forgetting credits.Enabled guard on new credit-related routes (both route and constructor levels)",
      "Passing nil handler to ServeHTTP \u2014 always guard optional handlers with nil checks or delegate to api.Unimplemented{}",
      "Adding a service to Config without adding it to Config.Validate() \u2014 silently accepts broken wiring",
      "Removing the var _ api.ServerInterface compile-time assertion"
    ],
    "decisions": [
      {
        "decision": "credits.enabled enforced at both NewServer (noop wiring) and route dispatch (explicit guard)",
        "rationale": "Credits is cross-cutting; a single guard point is insufficient. The route-level check is a safety net for cases where noop wiring is bypassed."
      },
      {
        "decision": "resolveNamespace is a closure, not a middleware",
        "rationale": "Namespace decoding is not a URL path parameter in v3; injecting it as a closure into each handler keeps the transport layer clean without requiring Chi context injection."
      },
      {
        "decision": "Handler fields are unexported; Config services are exported",
        "rationale": "Handlers are implementation details assembled in NewServer; the Config struct is the public dependency surface that callers (app/common) populate via Wire."
      }
    ],
    "code_examples": [
      {
        "scenario": "Adding a new domain handler to the v3 server",
        "code": "// 1. Add to Config in server.go:\nFooService foo.Service\n\n// 2. Add to Config.Validate():\nif c.FooService == nil { errs = append(errs, errors.New(\"foo service is required\")) }\n\n// 3. Add handler field to Server struct:\nfooHandler foohandler.Handler\n\n// 4. In NewServer, construct and assign:\nfooH := foohandler.New(resolveNamespace, config.FooService, httptransport.WithErrorHandler(config.ErrorHandler))\n// ...\nreturn &Server{ ..., fooHandler: fooH }, nil\n\n// 5. In routes.go, implement the generated interface method:\nfunc (s *Server) ListFoos(w http.ResponseWriter, r *http.Request, params api.ListFoosParams) {\n    s.fooHandler.ListFoos().With(params).ServeHTTP(w, r)\n}"
      }
    ]
  },
  "api/v3/templates": {
    "purpose": "A patch file that customises the oapi-codegen Chi middleware template to route deepObject 'filter' query parameters through a custom filters.Parse parser instead of the default runtime.BindQueryParameterWithOptions. This ensures union filter types are correctly parsed during code generation.",
    "patterns": [
      {
        "name": "Patch targets ParamName=filter + Style=deepObject exclusively",
        "description": "The template patch adds a conditional branch: only parameters named 'filter' with style 'deepObject' use the custom path. All other parameters continue using the standard oapi-codegen runtime binder.",
        "example": "{{- if and (eq .ParamName \"filter\") (eq .Style \"deepObject\") }}"
      }
    ],
    "key_file_guides": [
      {
        "file": "chi-middleware.tmpl.patch",
        "role": "Applied during make gen-api to override oapi-codegen's Chi middleware template for filter parameter handling.",
        "watch_for": "This patch must be re-applied after any oapi-codegen version upgrade. If filter params regress to standard binding, union filter types will silently break. The custom parser is filters.Parse from an imported package \u2014 ensure that import is present in the generated middleware."
      }
    ],
    "anti_patterns": [
      "Editing the generated chi-middleware output directly instead of maintaining this patch",
      "Widening the patch condition beyond ParamName=filter \u2014 standard deepObject params must use runtime.BindQueryParameterWithOptions"
    ],
    "decisions": [
      {
        "decision": "Custom parser injected via template patch rather than post-generation sed",
        "rationale": "Template patches survive oapi-codegen regeneration as long as the template structure is stable; post-generation edits are wiped on every make gen-api run."
      }
    ],
    "code_examples": []
  }
}