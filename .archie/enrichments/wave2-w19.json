{
  "pkg/framework/transport/httptransport": {
    "purpose": "Generic typed HTTP handler pipeline (decode \u2192 operation \u2192 encode) shared by all domain httpdriver packages. The single Handler[Request,Response] struct is the universal adapter between Chi routes and domain service calls; every v1 and v3 HTTP endpoint is built on it.",
    "patterns": [
      {
        "name": "NewHandler triple constructor",
        "description": "Always construct via NewHandler(requestDecoder, op, responseEncoder, ...options). Never instantiate handler{} directly \u2014 defaultHandlerOptions (GenericErrorEncoder) are injected by newHandler and would be missing.",
        "example": "httptransport.NewHandler(decodeListFoosRequest, operation.Operation[ListFoosRequest, ListFoosResponse](svc.List), encodeListFoosResponse, httptransport.WithOperationName(\"foo.list\"))"
      },
      {
        "name": "HandlerWithArgs for path-param injection",
        "description": "When a route parameter (e.g. Chi URL param) must be baked into the decoder, use NewHandlerWithArgs + .With(arg) at mount time. The With call clones the value-receiver handler, binding the arg into decodeRequest without allocation on each request.",
        "example": "h := httptransport.NewHandlerWithArgs(decodeGetFoo, op, encodeFoo); r.Get(\"/{id}\", func(w http.ResponseWriter, r *http.Request) { h.With(chi.URLParam(r, \"id\")).ServeHTTP(w, r) })"
      },
      {
        "name": "ErrorEncoder chain (first match wins)",
        "description": "WithErrorEncoder appends to h.errorEncoders; handlers iterate them in order; the first returning true short-circuits. GenericErrorEncoder is appended last by defaultHandlerOptions \u2014 custom domain encoders must be passed before it via options.",
        "example": "httptransport.NewHandler(dec, op, enc, httptransport.WithErrorEncoder(billingErrorEncoder), /* GenericErrorEncoder appended automatically */)"
      },
      {
        "name": "Chain for cross-cutting middleware",
        "description": "Use handler.Chain(outer, ...others) to wrap the operation with operation.Middleware (e.g. auth checks, rate limits). Chain returns a new handler value (value-receiver copy); the original is unmodified.",
        "example": "secured := h.Chain(authmiddleware.RequireScope(\"billing:write\"))"
      },
      {
        "name": "SelfEncodingError escape hatch",
        "description": "Errors that implement SelfEncodingError (EncodeError(ctx, w) bool) bypass the ErrorEncoder chain. Use only for error types that own their own HTTP status mapping (e.g. models.StatusProblem). Do not implement this on domain errors \u2014 use ErrorEncoder instead.",
        "example": "// models.StatusProblem implements SelfEncodingError and calls Respond(w) directly"
      }
    ],
    "key_file_guides": [
      {
        "file": "handler.go",
        "role": "Core Handler[Req,Resp] interface and internal handler struct. ServeHTTP orchestrates decode\u2192operation\u2192encode; encodeError iterates the chain then falls back to SelfEncodingError then 500.",
        "watch_for": "defaultHandlerOptions injects GenericErrorEncoder unconditionally \u2014 do not add a second GenericErrorEncoder via WithErrorEncoder or errors will be double-encoded."
      },
      {
        "file": "argshandler.go",
        "role": "HandlerWithArgs variant for route-param injection. With() clones the value-receiver handler and swaps decodeRequest; relies on handler being a non-pointer struct \u2014 if handler ever becomes a pointer, With() needs an explicit deep copy.",
        "watch_for": "Both With() and Chain() rely on value-receiver copy semantics. If you add pointer fields to handler, these methods will share state across copies."
      },
      {
        "file": "options.go",
        "role": "HandlerOption interface + all WithX constructors. HandlerOption.apply mutates handlerOptions; resolveErrorHandler returns dummyErrorHandler when none set.",
        "watch_for": "errorEncoders slice is order-sensitive (append). Passing domain-specific encoders after NewHandler has already appended GenericErrorEncoder (via defaultHandlerOptions) means the domain encoder never fires \u2014 pass custom encoders as options to NewHandler, not after Chain."
      },
      {
        "file": "encoder/encoder.go",
        "role": "Defines ResponseEncoder[T] and ErrorEncoder function types. Pure type definitions \u2014 no logic.",
        "watch_for": "ErrorEncoder must return false if it did not write to ResponseWriter; returning true after a partial write leaves the response in an inconsistent state."
      }
    ],
    "anti_patterns": [
      "Instantiating handler{} struct directly \u2014 bypasses defaultHandlerOptions and loses GenericErrorEncoder",
      "Calling context.Background() inside a RequestDecoder \u2014 always use the ctx passed from ServeHTTP (already carries request-scoped values and cancellation)",
      "Implementing SelfEncodingError on domain-level errors \u2014 reserve it for infrastructure error types; domain errors belong in ErrorEncoder chain",
      "Adding state (mutexes, caches) to HandlerWithArgs or handler \u2014 these are value types cloned by With() and Chain(); shared state would race",
      "Writing to ResponseWriter inside RequestDecoder on error \u2014 return the error and let the ErrorEncoder chain handle it; writing before encodeError breaks status code negotiation"
    ],
    "decisions": [
      {
        "decision": "Value-receiver handler with copy-on-With/Chain semantics",
        "rationale": "Enables safe per-request arg injection and middleware wrapping without allocation or locking; the original registered handler is immutable after construction."
      },
      {
        "decision": "ErrorEncoder chain with last-resort fallback to 500 in handler.go, not in GenericErrorEncoder",
        "rationale": "Keeps the fallback authoritative and unconfigurable; any error not handled by the chain or SelfEncodingError always produces a well-formed 500 problem+json response."
      },
      {
        "decision": "Separate encoder sub-package for ResponseEncoder/ErrorEncoder function types",
        "rationale": "Breaks the import cycle between handler.go (which uses both) and commonhttp (which defines GenericErrorEncoder) \u2014 encoder has no dependencies so both can import it."
      }
    ],
    "code_examples": [
      {
        "scenario": "Minimal domain HTTP handler wired into a Chi router",
        "code": "import (\n\t\"context\"\n\t\"net/http\"\n\n\t\"github.com/openmeterio/openmeter/pkg/framework/transport/httptransport\"\n\t\"github.com/openmeterio/openmeter/pkg/framework/transport/httptransport/encoder\"\n)\n\nfunc NewListFoosHandler(svc FooService) httptransport.Handler[ListFoosRequest, ListFoosResponse] {\n\treturn httptransport.NewHandler(\n\t\tfunc(ctx context.Context, r *http.Request) (ListFoosRequest, error) {\n\t\t\treturn ListFoosRequest{Namespace: r.Header.Get(\"X-Namespace\")}, nil\n\t\t},\n\t\tsvc.ListFoos,\n\t\tfunc(ctx context.Context, w http.ResponseWriter, r *http.Request, resp ListFoosResponse) error {\n\t\t\tw.Header().Set(\"Content-Type\", \"application/json\")\n\t\t\tw.WriteHeader(http.StatusOK)\n\t\t\treturn json.NewEncoder(w).Encode(resp)\n\t\t},\n\t\thttptransport.WithOperationName(\"foo.list\"),\n\t)\n}"
      }
    ]
  }
}