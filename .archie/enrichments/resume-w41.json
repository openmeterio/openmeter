{
  "pkg/ref": {
    "purpose": "Single-file utility providing IDOrKey \u2014 a discriminated union type that holds either a ULID-format ID or an arbitrary string key, used wherever domain lookups must accept both identifiers interchangeably.",
    "patterns": [
      {
        "name": "ULID-based discrimination",
        "description": "ParseIDOrKey uses ulid.Parse to decide which field to populate; a successful parse \u2192 ID, a parse error \u2192 Key. Never set both fields manually.",
        "example": "ref := ref.ParseIDOrKey(rawParam) // ID set if ULID, Key set otherwise"
      },
      {
        "name": "Validate before use",
        "description": "Always call Validate() before passing an IDOrKey into a service layer; it returns an error if both fields are empty.",
        "example": "if err := r.Validate(); err != nil { return err }"
      }
    ],
    "key_file_guides": [
      {
        "file": "ref.go",
        "role": "Defines IDOrKey struct with GetIDs, GetKeys, Validate, and ParseIDOrKey helpers.",
        "watch_for": "Callers must not set both ID and Key simultaneously; Validate only guards the all-empty case, not the both-set case."
      }
    ],
    "anti_patterns": [
      "Setting both ID and Key on an IDOrKey struct \u2014 semantics are undefined",
      "Skipping Validate() before service calls \u2014 silently passes empty identifiers",
      "Adding new identifier types here instead of defining a new discriminated union in the relevant domain package"
    ],
    "decisions": [
      {
        "decision": "Use ulid.Parse as the discriminator rather than a regex or length check",
        "rationale": "ULID has a canonical 26-char Crockford Base32 format; parse error is the cleanest way to distinguish it from freeform key strings without false positives."
      }
    ],
    "code_examples": []
  },
  "pkg/server": {
    "purpose": "Shared HTTP server primitives (request attribute extraction, structured logging middleware, powered-by header middleware) reused by both the v1 and v3 API servers; contains no business logic.",
    "patterns": [
      {
        "name": "OTel semconv attribute keys",
        "description": "Use go.opentelemetry.io/otel/semconv/v1.27.0 key constants for all HTTP attribute names; never hardcode string keys like 'http.method'.",
        "example": "string(semconv.HTTPRequestMethodKey): r.Method"
      },
      {
        "name": "Chi middleware.LogFormatter / LogEntry interfaces",
        "description": "RequestLogger implements middleware.LogFormatter and RequestLoggerEntry implements middleware.LogEntry; pass RequestLogger to middleware.RequestLogger() to integrate with Chi's request logging chain.",
        "example": "middleware.RequestLogger(&RequestLogger{Logger: handler})"
      },
      {
        "name": "Middleware factory pattern",
        "description": "Every middleware is exposed as a NewXxxMiddleware() func returning func(http.Handler) http.Handler, matching Chi's middleware signature.",
        "example": "func NewPoweredByMiddleware() func(next http.Handler) http.Handler { ... }"
      }
    ],
    "key_file_guides": [
      {
        "file": "attributes.go",
        "role": "Returns OTel-keyed attribute map for a request; requires middleware.RequestID to be in the middleware chain for req_id to be populated.",
        "watch_for": "req_id is absent silently if RequestID middleware is missing upstream."
      },
      {
        "file": "logger.go",
        "role": "Structured slog-based request logger satisfying Chi's middleware.LogFormatter interface.",
        "watch_for": "Log level is slog.LevelDebug for normal requests and slog.LevelError for panics; do not change to Info without considering log volume."
      },
      {
        "file": "middleware.go",
        "role": "Defines the shared MiddlewareFunc type alias used across v1 and v3 server wiring.",
        "watch_for": "Keep this type alias in sync with Chi's func(http.Handler) http.Handler; never wrap it in a struct."
      },
      {
        "file": "poweredby.go",
        "role": "Injects X-Powered-By header on every response.",
        "watch_for": "The header value 'OpenMeter by Kong, Inc.' is tested explicitly in poweredby_test.go; updating it is a breaking change for clients that parse this header."
      }
    ],
    "anti_patterns": [
      "Adding business logic or domain imports to this package \u2014 it must stay a pure infrastructure utility",
      "Hardcoding OTel attribute key strings instead of using semconv constants",
      "Returning errors from middleware constructors \u2014 they should panic on bad config or return a no-op handler"
    ],
    "decisions": [
      {
        "decision": "Log at DEBUG for normal requests, ERROR for panics",
        "rationale": "High-volume request logs would flood INFO level in production; DEBUG keeps them filterable without losing panic diagnostics."
      }
    ],
    "code_examples": []
  },
  "pkg/set": {
    "purpose": "Thread-safe generic set backed by map[T]struct{} with RWMutex; provides Add, Remove, AsSlice, IsEmpty, Subtract, and Union operations for use across any domain package needing concurrent set semantics.",
    "patterns": [
      {
        "name": "RWMutex for read/write separation",
        "description": "All mutating methods (Add, Remove) acquire a write lock; read methods (AsSlice, IsEmpty) acquire a read lock. Subtract and Union acquire read locks on all input sets before building the result.",
        "example": "s.mu.Lock(); defer s.mu.Unlock() // writes\ns.mu.RLock(); defer s.mu.RUnlock() // reads"
      },
      {
        "name": "Construct via New(items...)",
        "description": "Always use New[T]() or New(item1, item2) \u2014 never construct Set directly because the content map will be nil.",
        "example": "s := set.New(\"a\", \"b\")"
      }
    ],
    "key_file_guides": [
      {
        "file": "set.go",
        "role": "Full set implementation; Subtract and Union are package-level functions, not methods, to support multi-set operations.",
        "watch_for": "Subtract(a, b) removes items from a that are in b \u2014 the argument order is the same as lo.Difference(base, new), not the reverse."
      }
    ],
    "anti_patterns": [
      "Constructing Set{} directly (nil map panics on write)",
      "Calling AsSlice and assuming a stable order \u2014 iteration over a map is random",
      "Holding a reference to the returned slice from AsSlice and expecting it to reflect future mutations"
    ],
    "decisions": [
      {
        "decision": "Use RWMutex over sync.Map",
        "rationale": "RWMutex with a plain map is simpler and lower-overhead for the small sets typical in domain use; sync.Map is optimized for high-concurrency read-heavy workloads with minimal contention."
      }
    ],
    "code_examples": []
  },
  "pkg/slicesx": {
    "purpose": "Thin extension layer on top of samber/lo providing error-aware map, diff, and iteration helpers that lo omits; all functions are generic and pure (no side effects, no domain imports).",
    "patterns": [
      {
        "name": "MapWithErr collects all errors before returning",
        "description": "MapWithErr uses errors.Join to accumulate errors across all elements and returns nil slice + joined error if any mapping fails; it does NOT short-circuit on first error.",
        "example": "results, err := slicesx.MapWithErr(items, func(i Item) (Out, error) { return convert(i) })"
      },
      {
        "name": "Nil-in nil-out contract",
        "description": "Map and MapWithErr both return nil (not an empty slice) when given a nil input \u2014 callers relying on non-nil output must pass an initialized slice.",
        "example": "slicesx.Map(nil, f) // returns nil, not []S{}"
      },
      {
        "name": "NewDiff wraps lo.Difference with O(1) membership lookup",
        "description": "NewDiff(base, new) builds index maps for both additions and removals so Has/InAdditions/InRemovals are O(1).",
        "example": "d := slicesx.NewDiff(oldIDs, newIDs); if d.InAdditions(id) { ... }"
      },
      {
        "name": "Prefer lo for standard operations",
        "description": "Functions that lo already covers (Filter, Contains, Keys, etc.) should NOT be re-implemented here; add to slicesx only for patterns lo explicitly lacks.",
        "example": "// Use lo.Filter, not a custom slicesx.Filter"
      }
    ],
    "key_file_guides": [
      {
        "file": "diff.go",
        "role": "Diff[T,S] value type for comparing two slices; additions = items in base not in new, removals = items in new not in base (mirrors lo.Difference semantics).",
        "watch_for": "Diff argument order: NewDiff(base, new) \u2014 swapping them inverts additions/removals."
      },
      {
        "file": "map.go",
        "role": "Map (no-error) and MapWithErr (error-accumulating) generic slice transforms.",
        "watch_for": "MapWithErr skips erroring elements and continues \u2014 the result slice omits them; check for this when order or count matters."
      },
      {
        "file": "empty.go",
        "role": "EmptyAsNil normalises zero-length slices to nil for struct equality in tests.",
        "watch_for": "Only use in tests or assertion helpers; calling it in production can hide unintended empty-slice semantics."
      },
      {
        "file": "each.go",
        "role": "ForEachUntilWithErr iterates with early-exit support via a bool return from the callback.",
        "watch_for": "Callback returning (true, nil) breaks the loop without error; returning (false, err) stops with error \u2014 these are independent signals."
      }
    ],
    "anti_patterns": [
      "Duplicating lo functions (Filter, Contains, Uniq) here \u2014 use lo directly",
      "Using EmptyAsNil in production code paths \u2014 it is intended for test assertion normalisation only",
      "Adding stateful or side-effectful helpers \u2014 all functions here must be pure transforms"
    ],
    "decisions": [
      {
        "decision": "Wrap lo rather than replace it",
        "rationale": "lo covers ~90% of slice operations; slicesx fills the gaps (error-returning maps, diff with index, early-exit iteration) without duplicating the rest."
      }
    ],
    "code_examples": [
      {
        "scenario": "Convert a slice of domain entities to API types, collecting all conversion errors",
        "code": "import \"github.com/openmeterio/openmeter/pkg/slicesx\"\n\napiItems, err := slicesx.MapWithErr(domainItems, func(item domain.Entity) (api.Entity, error) {\n    return toAPIEntity(item)\n})\nif err != nil {\n    return nil, err\n}"
      }
    ]
  },
  "pkg/sortx": {
    "purpose": "Minimal package defining the sortx.Order string enum (ASC/DESC/empty) used as a shared sort-direction type across all domain list/query inputs.",
    "patterns": [
      {
        "name": "Use OrderDefault for unspecified sort direction",
        "description": "When a caller does not supply a sort direction, default to sortx.OrderDefault (= OrderAsc); never use the empty string literal directly.",
        "example": "order := sortx.OrderDefault"
      },
      {
        "name": "IsDefaultValue checks for unset, not for ASC",
        "description": "Order.IsDefaultValue() returns true when Order == OrderNone (empty string), not when Order == OrderAsc. The method name is misleading per the in-code TODO; use it only to detect absence of an explicit order.",
        "example": "if order.IsDefaultValue() { order = sortx.OrderDefault }"
      }
    ],
    "key_file_guides": [
      {
        "file": "order.go",
        "role": "Defines Order type and four constants: OrderAsc, OrderDesc, OrderDefault (=OrderAsc), OrderNone (empty).",
        "watch_for": "IsDefaultValue() checks for OrderNone, not OrderDefault \u2014 the name is acknowledged as misleading in a TODO comment."
      }
    ],
    "anti_patterns": [
      "Adding sort-field definitions or multi-column sort structs here \u2014 this package is intentionally minimal",
      "Comparing Order to string literals 'ASC'/'DESC' directly \u2014 use the constants",
      "Using IsDefaultValue() as a semantic check for 'is ascending' \u2014 it only detects unset"
    ],
    "decisions": [
      {
        "decision": "Single file, single type \u2014 no sub-packages",
        "rationale": "Sort direction is a leaf primitive shared by many list inputs; keeping it minimal prevents coupling and circular imports."
      }
    ],
    "code_examples": []
  }
}