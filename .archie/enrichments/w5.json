{
  "api/spec/packages/aip/common": {
    "purpose": "Organisational parent for shared OpenAPI YAML component fragments consumed by the v3 AIP TypeSpec build pipeline. Its single child (definitions/) centralises all reusable schemas, error responses, pagination metadata, filter types, and security schemes that every AIP route references via $ref.",
    "patterns": [],
    "key_file_guides": [
      {
        "file": "CLAUDE.md",
        "role": "Archie intent layer for the common/ organisational folder; describes the role of definitions/ and the centralisation decision.",
        "watch_for": "Do not add TypeSpec source or OpenAPI YAML fragments directly here \u2014 all component fragments belong in definitions/."
      }
    ],
    "anti_patterns": [
      "Adding OpenAPI YAML fragments directly in common/ instead of definitions/ \u2014 the build pipeline resolves $ref targets from definitions/ only",
      "Duplicating schema definitions across route-specific files instead of $ref-ing the shared definitions \u2014 breaks the single-source guarantee",
      "Adding route bindings or operation definitions here \u2014 this folder is shared infrastructure, not a route layer"
    ],
    "decisions": [
      {
        "decision": "All shared components live in a single definitions/ child rather than scattered alongside route files",
        "rationale": "Centralising $ref targets prevents duplicate schema definitions and ensures the TypeSpec build pipeline resolves all component references from one canonical location."
      }
    ],
    "code_examples": []
  },
  "api/spec/packages/aip/lib": {
    "purpose": "TypeSpec linter plugin for the v3 AIP spec \u2014 index.js exports a $linter (defineLinter) registering all custom rules (casing, docs, friendlyName, no-nullable, composition, field-prefix, operation-summary, operation-id) enforcing AIP naming conventions and structural constraints. Rules run during `make gen-api` and block or warn before any OpenAPI or SDK artifact is generated. The rules/ child holds the rule implementations.",
    "patterns": [
      {
        "name": "createRule factory for every rule",
        "description": "Every lint rule is defined via createRule from '@typespec/compiler'. Raw functions calling context.report directly are not registered by the rule engine.",
        "example": "export const myRule = createRule({ name: 'my-rule', severity: 'error', messages: { default: 'msg' }, create: (context) => ({ model: (m) => { /* ... */ } }) })"
      },
      {
        "name": "Two-severity model: error vs warning",
        "description": "Rules blocking compilation (missing @friendlyName, enum value casing) use severity 'error'; stylistic preferences (model name casing, prefix grouping, composition) use 'warning'. Never use error for stylistic-only rules.",
        "example": "export const friendlyNameRule = createRule({ severity: 'error', ... }); export const casingRule = createRule({ severity: 'warning', ... })"
      },
      {
        "name": "Register every rule in index.js",
        "description": "A new rule file has zero effect until imported and added to the rules array in index.js \u2014 the sole entry point consumed by defineLinter.",
        "example": "import { myRule } from './rules/my-rule.js'; const rules = [ ..., myRule ]; export const $linter = defineLinter({ rules })"
      },
      {
        "name": "PascalCase exceptions only in utils.js",
        "description": "Accepted acronym exceptions for isPascalCaseNoAcronyms (OAuth2, URL, API, UI, ID) live in utils.js \u2014 add new acronyms there, never inline in a rule.",
        "example": "const pascalCaseExceptions = ['OAuth2', 'URL', 'API', 'UI', 'ID']"
      },
      {
        "name": "EXCLUDED_PREFIXES for boolean-flag field groups",
        "description": "field-prefix.js exempts AIP-standard boolean-flag prefixes (is_, enable_, disable_, allow_) via EXCLUDED_PREFIXES; new boolean-flag prefixes must be added there to avoid spurious warnings.",
        "example": "const EXCLUDED_PREFIXES = ['is_', 'enable_', 'disable_', 'allow_', ...]"
      }
    ],
    "key_file_guides": [
      {
        "file": "index.js",
        "role": "Sole entry point \u2014 exports $linter via defineLinter, imports and registers every rule (casing, casingErrors, doc, docFormat, friendlyName, noNullable, operationSummary, operationIdKebabCase, compositionOverInheritance, repeatedPrefixGrouping).",
        "watch_for": "Adding a new rule file without importing it here \u2014 the rule is silently ignored."
      },
      {
        "file": "rules/utils.js",
        "role": "Shared casing helpers (isPascalCaseNoAcronyms, isCamelCaseNoAcronyms, isSnakeCase, isKebabCase) and the pascalCaseExceptions allowlist.",
        "watch_for": "Acronyms missing from pascalCaseExceptions are flagged as casing errors \u2014 add them here, not per-rule."
      },
      {
        "file": "rules/casing.js",
        "role": "casingRule (warning) for model/enum/property casing; casingErrorsRule (error) requiring enum member string values to be snake_case.",
        "watch_for": "@path-decorated params are checked for camelCase not snake_case \u2014 renaming them to silence a warning produces a new error."
      },
      {
        "file": "rules/friendly-name.js",
        "role": "Enforces @friendlyName on all models/enums/unions/non-Endpoints-Operations interfaces (error); *Endpoints/*Operations interfaces must NOT have @friendlyName.",
        "watch_for": "Every new model, enum, union, and non-route interface needs @friendlyName \u2014 missing it blocks gen-api."
      },
      {
        "file": "rules/field-prefix.js",
        "role": "Warns when 2+ properties share a common prefix_ and should be grouped; EXCLUDED_PREFIXES exempts AIP boolean-flag prefixes.",
        "watch_for": "New boolean-flag property groups without an EXCLUDED_PREFIXES entry produce spurious warnings."
      },
      {
        "file": "rules/composition-over-inheritance.js",
        "role": "Warns when a model extends a base without @discriminator; prefers spread (...Base) or 'model is Base'.",
        "watch_for": "Using 'extends' for mixins without @discriminator produces warnings \u2014 prefer spread syntax."
      },
      {
        "file": "rules/docs.js",
        "role": "Enforces @doc on models/enums/properties; docFormatRule checks format conventions.",
        "watch_for": "Missing @doc on new types blocks gen-api if configured as error severity."
      }
    ],
    "anti_patterns": [
      "Defining a rule as a raw function calling context.report directly instead of using createRule \u2014 TypeSpec rule registration requires createRule",
      "Using severity 'error' for stylistic preferences (casing, prefix grouping) \u2014 errors block gen-api; use 'warning'",
      "Adding new accepted PascalCase acronyms anywhere other than pascalCaseExceptions in utils.js",
      "Forgetting to import a new rule in index.js \u2014 the rule file is valid JS but has zero effect",
      "Adding underscore-prefixed boolean-flag field groups without adding the prefix to EXCLUDED_PREFIXES in field-prefix.js"
    ],
    "decisions": [
      {
        "decision": "Two-severity model: error blocks compilation, warning surfaces in lint output but does not fail gen-api",
        "rationale": "Hard naming contracts (friendlyName, enum value casing) must be enforced before code generation; stylistic preferences should guide authors without halting every iteration."
      },
      {
        "decision": "PascalCase exceptions codified in utils.js rather than inline per rule",
        "rationale": "Centralising exceptions makes them reviewable and extensible without touching each rule using isPascalCaseNoAcronyms."
      },
      {
        "decision": "EXCLUDED_PREFIXES in field-prefix.js exempts AIP-standard boolean-flag prefixes",
        "rationale": "AIP guidelines explicitly allow these prefix patterns for boolean fields; grouping them into sub-objects would violate AIP conventions."
      }
    ],
    "code_examples": []
  }
}