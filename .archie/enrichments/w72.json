{
  "openmeter/billing/validators/customer": {
    "purpose": "Implements customer.RequestValidator to enforce billing pre-conditions before customer deletion \u2014 blocks removal of customers with non-final standard invoices or active (non-soft-deleted) gathering invoices. The cross-domain guard that prevents customer removal from leaving orphaned billing state.",
    "patterns": [
      {
        "name": "Embed NoopRequestValidator",
        "description": "Validator embeds customer.NoopRequestValidator so only ValidateDeleteCustomer needs implementation; other methods default to no-op.",
        "example": "type Validator struct { customer.NoopRequestValidator; billingService billing.Service; syncService subscriptionsync.Service; subscriptionService subscription.Service }"
      },
      {
        "name": "Compile-time interface assertion",
        "description": "Package-level var _ check ensures the struct satisfies the interface.",
        "example": "var _ customer.RequestValidator = (*Validator)(nil)"
      },
      {
        "name": "Sync-before-validate",
        "description": "ValidateDeleteCustomer syncs active subscriptions via SynchronizeSubscription before checking invoice state so pending charges are reflected.",
        "example": "v.syncService.SynchronizeSubscription(ctx, view, time.Now())"
      },
      {
        "name": "Nil-guard constructors",
        "description": "NewValidator returns an error if any required service dependency is nil \u2014 enforced at construction, not call time.",
        "example": "if billingService == nil { return nil, fmt.Errorf(\"billing service is required\") }"
      },
      {
        "name": "errors.Join for multi-invoice failures",
        "description": "Invoice validation failures accumulate into a slice and join via errors.Join so the caller gets all blocking invoices at once.",
        "example": "errs = append(errs, fmt.Errorf(\"invoice %s is not in final state\", stdInvoice.ID)); return errors.Join(errs...)"
      }
    ],
    "key_file_guides": [
      {
        "file": "customer.go",
        "role": "Single-file package: Validator struct, NewValidator, and ValidateDeleteCustomer (the only NoopRequestValidator override).",
        "watch_for": "24h watermark (time.Now().Add(-24*time.Hour)) is an embedded rule for active-subscription detection; soft-deleted gathering invoices (DeletedAt != nil) are skipped; standard invoices must be IsFinal(); all reads go through billing.Service/subscription.Service, never adapters."
      }
    ],
    "anti_patterns": [
      "Adding ValidateCreateCustomer/ValidateUpdateCustomer overrides without a billing reason \u2014 this package only guards delete.",
      "Calling billing adapter or Ent directly instead of billing.Service.",
      "Skipping SynchronizeSubscription before invoice validation \u2014 gate check becomes incorrect.",
      "Using context.Background() instead of propagating the incoming ctx.",
      "Returning a plain error instead of accumulating with errors.Join \u2014 callers need all blocking invoices."
    ],
    "decisions": [
      {
        "decision": "Validator registers via customer.Service.RegisterRequestValidator() at wiring time in app/common, not via direct import.",
        "rationale": "Billing depends on customer; customer cannot import billing. Wire-time registration inverts the dependency without a cycle."
      },
      {
        "decision": "Subscription sync is driven inside the validator, not as a subscription-service pre-hook.",
        "rationale": "The validator needs a billing-consistent invoice snapshot; syncing at validation time makes the check atomic with the delete."
      }
    ],
    "code_examples": [
      {
        "scenario": "Add a new pre-delete billing check (block deletion if open credit notes exist)",
        "code": "creditNotes, err := v.billingService.ListCreditNotes(ctx, billing.ListCreditNotesInput{\n    Namespaces: []string{input.Namespace}, Customers: []string{input.ID},\n})\nif err != nil { return err }\nfor _, cn := range creditNotes.Items {\n    if !cn.Status.IsFinal() {\n        errs = append(errs, fmt.Errorf(\"credit note %s is not final\", cn.ID))\n    }\n}\nreturn errors.Join(errs...)"
      }
    ]
  },
  "openmeter/billing/validators/subscription": {
    "purpose": "Implements subscription.SubscriptionCommandHook to validate that a customer's billing app setup is valid whenever a subscription with billable items is created or updated \u2014 blocks lifecycle transitions when the invoicing app cannot handle tax calculation, invoicing, or payment collection.",
    "patterns": [
      {
        "name": "Embed NoOpSubscriptionCommandHook",
        "description": "Validator embeds subscription.NoOpSubscriptionCommandHook so only AfterCreate/AfterUpdate need implementation.",
        "example": "type Validator struct { subscription.NoOpSubscriptionCommandHook; billingService billing.Service }"
      },
      {
        "name": "Early-exit on non-billable subscription",
        "description": "hasBillableItems checks whether any rate card has a non-nil Price before calling the billing service, avoiding network calls for free-tier subscriptions.",
        "example": "if !v.hasBillableItems(view) { return nil }"
      },
      {
        "name": "Conflict error wrapping",
        "description": "Billing validation failures wrap in models.NewGenericConflictError, mapping to HTTP 409.",
        "example": "return models.NewGenericConflictError(fmt.Errorf(\"invalid billing setup: %w\", err))"
      },
      {
        "name": "Return interface from constructor",
        "description": "NewValidator returns subscription.SubscriptionCommandHook (interface), not *Validator.",
        "example": "func NewValidator(billingService billing.Service) (subscription.SubscriptionCommandHook, error)"
      },
      {
        "name": "Capability slice validation",
        "description": "Required app capabilities are passed as a slice to ValidateCustomer, keeping validation app-agnostic.",
        "example": "customerApp.ValidateCustomer(ctx, customer, []app.CapabilityType{app.CapabilityTypeCalculateTax, app.CapabilityTypeInvoiceCustomers, app.CapabilityTypeCollectPayments})"
      }
    ],
    "key_file_guides": [
      {
        "file": "validator.go",
        "role": "Single-file package: Validator, NewValidator, AfterCreate, AfterUpdate, validateBillingSetup, hasBillableItems. Both After* delegate to validateBillingSetup.",
        "watch_for": "customerapp.AsCustomerApp type-asserts the app base \u2014 fails at runtime if the app type changes; GetCustomerOverrideInput.Expand must set both Apps:true and Customer:true."
      }
    ],
    "anti_patterns": [
      "Overriding BeforeCreate/BeforeDelete without strong justification \u2014 post-create/update validation runs against the fully persisted view intentionally.",
      "Returning a plain error instead of models.NewGenericConflictError for billing setup failures.",
      "Calling subscription service or Ent adapters directly \u2014 billing reads go through billing.Service.",
      "Adding app-type-specific branching \u2014 capability validation is intentionally app-agnostic.",
      "Using context.Background() instead of propagating the incoming ctx."
    ],
    "decisions": [
      {
        "decision": "Validator registers via subscription.Service.RegisterHook() at wiring time in app/common, not via direct import.",
        "rationale": "Billing imports subscription; subscription cannot import billing. Wire-time registration inverts the dependency without a cycle."
      },
      {
        "decision": "Validation runs after create/update, not before.",
        "rationale": "The billing service needs the fully persisted subscription view (all phases/items resolved) to evaluate billability; a before-hook lacks this view."
      }
    ],
    "code_examples": [
      {
        "scenario": "Add a new required billing capability check",
        "code": "// In validateBillingSetup, extend the capability slice:\nreturn customerApp.ValidateCustomer(ctx, customerProfile.Customer, []app.CapabilityType{\n    app.CapabilityTypeCalculateTax,\n    app.CapabilityTypeInvoiceCustomers,\n    app.CapabilityTypeCollectPayments,\n    app.CapabilityTypeRefundPayments, // new capability\n})"
      }
    ]
  }
}