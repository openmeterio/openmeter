{
  "pkg/log": {
    "purpose": "Provides two orthogonal logging utilities: a testify-based mock for slog.Handler used in unit tests, and a PanicLogger recover-and-log helper for use in goroutine entry points. Primary constraint: this is infrastructure glue, not a logger factory \u2014 callers get slog.Default() for actual logging.",
    "patterns": [
      {
        "name": "PanicLogger via defer",
        "description": "Always invoke PanicLogger as a deferred call at the top of a goroutine or main, passing one propagation option (WithRePanic, WithExit, or WithContinue).",
        "example": "defer log.PanicLogger(log.WithExit)"
      },
      {
        "name": "OTel stack trace attribute",
        "description": "Stack traces are logged under the OTelCodeStackTrace constant key (\"code.stacktrace\") per OpenTelemetry semantic conventions \u2014 never use a different key for stack traces.",
        "example": "slog.Error(description, OTelCodeStackTrace, string(debug.Stack()))"
      },
      {
        "name": "MockHandler satisfies slog.Handler compile check",
        "description": "var _ slog.Handler = &MockHandler{} is declared to enforce interface compliance at compile time \u2014 do the same for any new mock in this package.",
        "example": "var _ slog.Handler = &MockHandler{}"
      }
    ],
    "key_file_guides": [
      {
        "file": "panic.go",
        "role": "Recover-and-log wrapper for goroutine panic handling; exposes three propagation strategies.",
        "watch_for": "Default propagation strategy (zero value) is RePanic \u2014 omitting any option re-panics, which is rarely what you want in a worker; pass WithExit or WithContinue explicitly."
      },
      {
        "file": "mock.go",
        "role": "testify mock for slog.Handler; use only in tests.",
        "watch_for": "MockHandler.WithAttrs and WithGroup return values are obtained via type assertion on mock.Called \u2014 if the mock is not set up, the assertion will panic."
      }
    ],
    "anti_patterns": [
      "Adding a custom logger constructor here \u2014 callers should use slog.Default() or inject slog.Logger externally.",
      "Using a raw string instead of OTelCodeStackTrace for stack trace attribute keys.",
      "Invoking PanicLogger without a defer \u2014 it must run in a recover() scope."
    ],
    "decisions": [
      {
        "decision": "Three named propagation strategies instead of a boolean re-panic flag",
        "rationale": "Workers (WithContinue), long-running servers (WithExit), and test helpers (WithRePanic) have different needs; a single boolean would conflate exit vs continue."
      }
    ],
    "code_examples": []
  },
  "pkg/lrux": {
    "purpose": "Generic LRU cache with per-item TTL and a synchronous Fetcher callback, wrapping hashicorp/golang-lru/v2. Primary constraint: all time comparisons use pkg/clock (not time.Now) so tests can freeze time.",
    "patterns": [
      {
        "name": "Clock package for time",
        "description": "All calls to time.Now() are replaced with clock.Now() so the cache TTL logic is testable with clock.FreezeTime / clock.SetTime.",
        "example": "item.ExpiresAt.After(clock.Now())"
      },
      {
        "name": "Functional options for construction",
        "description": "Constructor accepts variadic cacheOptionsFunc; add new options by defining a func(*cacheOptions) and exposing a With* factory function.",
        "example": "func WithTTL(ttl time.Duration) cacheOptionsFunc { return func(o *cacheOptions) { o.ttl = ttl } }"
      },
      {
        "name": "Zero TTL means no expiry",
        "description": "When ttl == 0, ExpiresAt is set to clock.Now().Add(0) == Now, but the check is item.ExpiresAt.IsZero() || After(clock.Now()), so a truly zero ExpiresAt (time.Time{}) means never-expire \u2014 set ttl=0 intentionally for this behavior.",
        "example": "if ok && (item.ExpiresAt.IsZero() || item.ExpiresAt.After(clock.Now())) {"
      },
      {
        "name": "Fetcher must not be nil",
        "description": "Constructor returns an error if fetcher is nil; always validate the fetcher before calling NewCacheWithItemTTL.",
        "example": "if fetcher == nil { return nil, fmt.Errorf(\"fetcher is required\") }"
      }
    ],
    "key_file_guides": [
      {
        "file": "lruitemttl.go",
        "role": "Entire package implementation \u2014 CacheWithItemTTL, Fetcher type, NewCacheWithItemTTL, Get, Refresh.",
        "watch_for": "No mutex around fetch+store; concurrent Gets on an expired key will issue multiple fetcher calls. This is documented as intentional but must be considered for expensive fetchers."
      },
      {
        "file": "lruitemttl_test.go",
        "role": "Unit tests using clock freeze to validate TTL boundary behavior and error propagation.",
        "watch_for": "Tests use t.Context() (not context.Background()) and defer clock.UnFreeze() \u2014 new tests must follow the same pattern."
      }
    ],
    "anti_patterns": [
      "Using time.Now() inside a Fetcher or anywhere in this package \u2014 always use clock.Now().",
      "Adding a mutex around Get without benchmarking \u2014 the current design intentionally allows stampede for simplicity.",
      "Constructing with a nil fetcher \u2014 the constructor rejects it but callers should validate early."
    ],
    "decisions": [
      {
        "decision": "Wrap hashicorp/golang-lru with CacheItemWithTTL instead of using lru's built-in expiry",
        "rationale": "Allows TTL checks to go through pkg/clock for deterministic test control, which lru's native eviction does not support."
      }
    ],
    "code_examples": [
      {
        "scenario": "Create a cache that fetches string values by string key with a 10-second TTL",
        "code": "import (\n\t\"context\"\n\t\"github.com/openmeterio/openmeter/pkg/lrux\"\n\t\"time\"\n)\n\ncache, err := lrux.NewCacheWithItemTTL(100, func(ctx context.Context, key string) (string, error) {\n\treturn fetchFromDB(ctx, key)\n}, lrux.WithTTL(10*time.Second))\nif err != nil { ... }\nval, err := cache.Get(ctx, \"mykey\")"
      }
    ]
  },
  "pkg/otelx": {
    "purpose": "Minimal OpenTelemetry span-context serialization helpers for cross-process propagation (e.g. via Kafka message headers). Serializes trace.SpanContext to/from base64-encoded JSON using the OTelSpanContextKey constant.",
    "patterns": [
      {
        "name": "Base64-over-JSON encoding",
        "description": "SpanContext is marshalled to JSON via trace.SpanContext.MarshalJSON() and then base64-encoded; reversal uses base64.DecodeString then json.Unmarshal into trace.SpanContextConfig.",
        "example": "s := base64.StdEncoding.EncodeToString(b)"
      },
      {
        "name": "OTelSpanContextKey constant for header/attribute name",
        "description": "Always use the OTelSpanContextKey constant (\"otel.span.context\") as the key when storing or reading the serialized span context \u2014 do not hardcode the string.",
        "example": "const OTelSpanContextKey = \"otel.span.context\""
      }
    ],
    "key_file_guides": [
      {
        "file": "spancontext.go",
        "role": "Only file; exposes SerializeSpanContext and DeserializeSpanContext plus the key constant.",
        "watch_for": "DeserializeSpanContext returns *trace.SpanContext (pointer); callers must nil-check before dereferencing."
      }
    ],
    "anti_patterns": [
      "Storing the serialized span context under a key other than OTelSpanContextKey.",
      "Using W3C TraceContext or B3 propagators here \u2014 this package uses its own JSON/base64 scheme for non-HTTP transports (e.g. Kafka headers)."
    ],
    "decisions": [
      {
        "decision": "Custom base64+JSON serialization instead of standard W3C propagator",
        "rationale": "Kafka message headers carry opaque byte slices; W3C propagator expects HTTP carrier semantics. Base64 over JSON is self-contained and transport-agnostic."
      }
    ],
    "code_examples": []
  },
  "pkg/pgxpoolobserver": {
    "purpose": "Single-function OTel metrics registration for pgxpool.Pool, exposing 13 pool statistics as observable gauges/counters. Call once per pool at startup; the registered callback polls pool.Stat() on each collection cycle.",
    "patterns": [
      {
        "name": "Register all observables in one RegisterCallback call",
        "description": "All metric.Observable instances are collected into a slice and passed to meter.RegisterCallback in a single call \u2014 do not register separate callbacks per metric.",
        "example": "_, err = meter.RegisterCallback(func(_ context.Context, o metric.Observer) error { ... }, allMetrics...)"
      },
      {
        "name": "additionalAttributes variadic for label injection",
        "description": "Callers pass extra attribute.KeyValue pairs (e.g. db name, instance ID) via the variadic additionalAttributes parameter; every ObserveInt64 call appends them.",
        "example": "o.ObserveInt64(acquireCountMetric, acquireCount, metric.WithAttributes(additionalAttributes...))"
      },
      {
        "name": "Guard derived metrics against division by zero",
        "description": "avgAcquiredDurationMetric is only observed when acquireCount > 0 \u2014 replicate this guard for any derived ratio metrics.",
        "example": "if acquireCount > 0 { o.ObserveInt64(avgAcquiredDurationMetric, acquireDurationMS/acquireCount, ...) }"
      }
    ],
    "key_file_guides": [
      {
        "file": "observer.go",
        "role": "Entire package; exposes only ObservePoolMetrics(meter, pool, ...attributes).",
        "watch_for": "Error from each meter.Int64ObservableCounter/Gauge registration is checked individually and returned immediately \u2014 all 13 registrations must succeed or the function returns an error."
      }
    ],
    "anti_patterns": [
      "Calling ObservePoolMetrics more than once for the same pool/meter pair \u2014 duplicate metric registration will error.",
      "Adding new metrics without appending to allMetrics \u2014 they will not be observed in the callback."
    ],
    "decisions": [
      {
        "decision": "Observable push model (RegisterCallback) instead of explicit Record calls",
        "rationale": "Pool stats are instantaneous gauge values polled by the metrics collector; push-on-collection avoids background goroutines and unnecessary polling when no collector is active."
      }
    ],
    "code_examples": []
  },
  "pkg/redis": {
    "purpose": "Thin factory layer for go-redis/v9 client construction from a Viper-backed Config struct, with built-in OTel tracing and metrics instrumentation via redisotel. Supports standalone and Sentinel topologies.",
    "patterns": [
      {
        "name": "Config.Validate before NewClient",
        "description": "Config.Validate() uses errors.Join to accumulate field errors; always call Validate() before constructing the client in application wiring.",
        "example": "if err := cfg.Validate(); err != nil { return err }"
      },
      {
        "name": "Functional options for OTel providers",
        "description": "OTel TracerProvider and MeterProvider are injected via Option funcs (WithTracingProvider, WithMeterProvider) layered on top of Options; nil providers are silently ignored.",
        "example": "NewClient(Options{Config: cfg}, WithTracingProvider(tp), WithMeterProvider(mp))"
      },
      {
        "name": "Configure sets Viper defaults with prefix",
        "description": "Call Configure(v, prefix) to register all Redis defaults under a namespaced Viper prefix (e.g. \"redis\") \u2014 do not set defaults inline in app config.",
        "example": "redis.Configure(v, \"dedupe.redis\")"
      },
      {
        "name": "TLS minimum version is TLS 1.3",
        "description": "When TLS.Enabled is true, the client always sets MinVersion: tls.VersionTLS13 \u2014 do not override this in callers.",
        "example": "tlsConfig = &tls.Config{InsecureSkipVerify: o.TLS.InsecureSkipVerify, MinVersion: tls.VersionTLS13}"
      }
    ],
    "key_file_guides": [
      {
        "file": "client.go",
        "role": "NewClient factory: applies options, builds TLS config, constructs standalone or Sentinel client, instruments tracing and metrics.",
        "watch_for": "redisotel.InstrumentTracing and InstrumentMetrics are called even when providers are nil (opts slice is empty but call still happens) \u2014 do not short-circuit these calls."
      },
      {
        "file": "config.go",
        "role": "Config struct, Validate, Configure (Viper defaults), and Config.NewClient convenience method.",
        "watch_for": "Config.NewClient() does not accept OTel options \u2014 use NewClient(Options{Config: c}, WithTracingProvider(...)) directly when telemetry is needed."
      }
    ],
    "anti_patterns": [
      "Constructing go-redis client directly in application code instead of via this package \u2014 OTel instrumentation would be missing.",
      "Calling Configure with an empty prefix \u2014 all keys would collide at the root Viper namespace.",
      "Ignoring Config.Validate() \u2014 an empty Address will produce a client that fails at first use, not at construction."
    ],
    "decisions": [
      {
        "decision": "OTel instrumentation is always attempted, not gated on provider presence",
        "rationale": "redisotel.InstrumentTracing/Metrics are no-ops when no provider is passed, so always calling them avoids conditional branching and ensures new callers get observability automatically when they wire providers."
      }
    ],
    "code_examples": []
  }
}