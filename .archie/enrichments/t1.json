{
  "api/client": {
    "purpose": "Public SDK surface for OpenMeter \u2014 organises generated and hand-authored client code for Go (api/client/go), JavaScript npm package @openmeter/sdk (api/client/javascript), and Python (api/client/python). All business logic and API types originate from generated artefacts; this folder only wires them into stable, externally-importable packages. api/client/node and api/client/web are deprecated tombstones.",
    "patterns": [
      {
        "name": "Generated-first, wrapper-second",
        "description": "All API types, request/response structs, and operation methods live in generated files. Hand-authored files add ergonomic wrappers and auth helpers only.",
        "example": "client.go adds WithAPIKey() and ergonomic List* helpers atop the generated ClientWithResponses \u2014 never adds new types."
      },
      {
        "name": "Regeneration via make gen-api only",
        "description": "client.gen.go, src/client/schemas.ts, and openmeter/_generated/ are overwritten on every `make gen-api` run. No manual edits survive.",
        "example": "codegen.yaml drives oapi-codegen for Go; scripts/generate.ts drives openapi-typescript + orval for JS."
      },
      {
        "name": "Auth via RequestEditorFn / ClientOption (Go)",
        "description": "Authentication tokens are injected via the RequestEditorFn / ClientOption pattern in client.go, never embedded inside generated operation methods.",
        "example": "WithAPIKey(token string) ClientOption wraps the generated client with a bearer-token RequestEditorFn."
      }
    ],
    "key_file_guides": [
      {
        "file": "api/client/go/client.gen.go",
        "role": "Generated Go SDK \u2014 oapi-codegen output from api/openapi.cloud.yaml. Never edit.",
        "watch_for": "Enum types carry Valid(); always-prefix-enum-values codegen option is set in codegen.yaml."
      },
      {
        "file": "api/client/go/client.go",
        "role": "Hand-authored ergonomic wrappers and auth helpers on top of client.gen.go.",
        "watch_for": "Must not import app-internal monorepo packages; must remain externally importable."
      },
      {
        "file": "api/client/javascript/package.json",
        "role": "Declares four named sub-package exports (default, /portal, /react, /zod) and the dual ESM/CJS duel build.",
        "watch_for": "New resource classes must also be registered as public fields on the root OpenMeter class in src/client/index.ts."
      },
      {
        "file": "api/client/javascript/patches/openapi-typescript.patch",
        "role": "Single consolidated patch applied to openapi-typescript dist/ at install time.",
        "watch_for": "pnpm patchedDependencies expects exactly one patch file; adding a second breaks install."
      },
      {
        "file": "api/client/python/openmeter/_client.py",
        "role": "Hand-authored Python client subclassing generated OpenMeterClient; _patch_sdk() called at module end.",
        "watch_for": "All operations must come from openmeter/_generated/; never define new operation methods here."
      },
      {
        "file": "api/client/node/README.md",
        "role": "Tombstone \u2014 Node SDK deprecated in favour of api/client/javascript.",
        "watch_for": "Do not add code here."
      },
      {
        "file": "api/client/web/README.md",
        "role": "Tombstone \u2014 Web SDK deprecated in favour of api/client/javascript.",
        "watch_for": "Do not add code here."
      }
    ],
    "anti_patterns": [
      "Manually editing any *.gen.go, src/client/schemas.ts, or openmeter/_generated/ file \u2014 overwritten by make gen-api",
      "Adding new API types or request/response structs to the hand-authored wrapper files instead of api/spec/ TypeSpec",
      "Importing app-internal monorepo packages into Go or Python SDK \u2014 must remain externally importable",
      "Adding new SDK code to api/client/node or api/client/web \u2014 both are deprecated tombstones",
      "Writing tests that bypass httptest.NewServer and make live HTTP calls (Go) or run scripts/generate.ts from outside the package root (JS)"
    ],
    "decisions": [
      {
        "decision": "Go SDK generated from api/openapi.cloud.yaml (not openapi.yaml)",
        "rationale": "Cloud spec includes cloud-specific auth and endpoint variants; SDK consumers target the hosted service."
      },
      {
        "decision": "Python client subclasses generated OpenMeterClient rather than wrapping it",
        "rationale": "Allows _patch_sdk() to augment generated methods cleanly while keeping the public API surface identical to the generated class."
      },
      {
        "decision": "Four named JS sub-package exports instead of a single entry",
        "rationale": "Lets consumers tree-shake; portal client is intentionally scoped to portal-token operations only."
      }
    ],
    "code_examples": []
  },
  "api/spec": {
    "purpose": "Single source of truth for all OpenMeter HTTP APIs. TypeSpec packages under packages/ compile to api/openapi.yaml (v1), api/openapi.cloud.yaml (v1 cloud), and api/v3/openapi.yaml (v3 AIP). All Go server stubs, Go/JS/Python SDKs are downstream \u2014 never edit generated artefacts directly.",
    "patterns": [
      {
        "name": "Two-package split: aip vs legacy",
        "description": "packages/aip/ owns v3 AIP-style spec (tspconfig compiles to api/v3/openapi.yaml); packages/legacy/ owns v1/v2 spec (compiles to api/openapi.yaml and api/openapi.cloud.yaml). Never mix v3 content into legacy/ or v1 content into aip/.",
        "example": "New billing v3 endpoint: add .tsp file under packages/aip/src/, import in openmeter.tsp root namespace."
      },
      {
        "name": "Route/tag binding at root namespace only",
        "description": "Domain sub-folder .tsp files declare models and operations; @route and @tag decorators are bound only in root namespace files (openmeter.tsp, konnect.tsp, main.tsp).",
        "example": "packages/aip/src/openmeter.tsp imports sub-domain files and applies @route at the top-level interface."
      },
      {
        "name": "make gen-api is the only regeneration path",
        "description": "Running pnpm generate \u2192 pnpm --filter compile \u2192 openapi bundle \u2192 cp produces all output artefacts. Direct tsc or tsp compile calls skip the yq filter-schema loop and flatten-allof post-processing.",
        "example": "Makefile generate target: pnpm --filter @openmeter/api-spec-aip run compile \u2192 openapi bundle \u2192 ../../../v3/openapi.yaml"
      },
      {
        "name": "Vendored patches applied via pnpm patchedDependencies",
        "description": "TypeSpec compiler/emitter bugs are fixed via patches/ files applied at npm install time. Each patch targets compiled dist/ not TypeScript src/.",
        "example": "package.json pnpm.patchedDependencies maps @typespec/compiler to patches/@typespec__compiler.patch"
      },
      {
        "name": "New legacy sub-domain file must register in both main.tsp and cloud/main.tsp",
        "description": "packages/legacy/src/main.tsp and packages/legacy/src/cloud/main.tsp are the compilation entry points; files not imported there are silently ignored.",
        "example": "Adding packages/legacy/src/myfeature.tsp: add `import './myfeature.tsp';` to both main.tsp files."
      }
    ],
    "key_file_guides": [
      {
        "file": "api/spec/packages/aip/src/openmeter.tsp",
        "role": "Root namespace file for v3 OpenMeter spec; all @route/@tag bindings live here.",
        "watch_for": "Adding @route inside sub-domain files breaks the routing contract."
      },
      {
        "file": "api/spec/packages/aip/scripts/flatten-allof.mjs",
        "role": "Post-processing step that flattens allOf in the AIP output YAML; runs after tsp compile.",
        "watch_for": "If upstream TypeSpec fixes allOf generation this script may become a no-op but must not be deleted without verification."
      },
      {
        "file": "api/spec/packages/legacy/src/main.tsp",
        "role": "Entry point for v1/v2 OpenMeter compilation.",
        "watch_for": "New sub-domain files must be imported here AND in cloud/main.tsp."
      },
      {
        "file": "api/spec/packages/legacy/src/types.tsp",
        "role": "Shared primitive types (ULID, DateTime, Resource) for v1 spec.",
        "watch_for": "Never re-declare these in sub-domain files."
      },
      {
        "file": "api/spec/Makefile",
        "role": "Canonical generate/lint/format targets; called by root `make gen-api`.",
        "watch_for": "The yq filter-schema loop replaces inline filter definitions with $ref after compile \u2014 filter schemas must match expected names."
      },
      {
        "file": "api/spec/package.json",
        "role": "Workspace root; declares TypeSpec version pins and patchedDependencies.",
        "watch_for": "Bumping a patched package version requires updating the corresponding patch file hash; re-test full make gen-api pipeline."
      },
      {
        "file": "api/spec/patches/",
        "role": "Vendored patch files (patch-package format) applied to TypeSpec compiler/emitter packages at install time.",
        "watch_for": "Patch compiled dist/ not TypeScript src/; one concern per patch; remove stale patches when upstream ships the fix."
      }
    ],
    "anti_patterns": [
      "Hand-editing api/openapi.yaml, api/openapi.cloud.yaml, or api/v3/openapi.yaml \u2014 always regenerate via make gen-api",
      "Declaring @route or @tag inside domain sub-folder operation files \u2014 routing belongs in root namespace files only",
      "Patching TypeScript source files (src/**/*.ts) in patches/ \u2014 node_modules ships dist/ only; source patches are silently ignored",
      "Adding v3 domain content into packages/legacy/ or v1 content into packages/aip/",
      "Adding a new legacy sub-domain file without registering it in both main.tsp and cloud/main.tsp"
    ],
    "decisions": [
      {
        "decision": "Two separate packages (aip/ and legacy/) with independent tspconfig files",
        "rationale": "v3 AIP spec and v1 legacy spec have different emitter configurations, versioning strategies, and output targets; a single package would require complex conditional compilation."
      },
      {
        "decision": "Post-processing flatten-allof.mjs in aip/ rather than upstream TypeSpec changes",
        "rationale": "Upstream TypeSpec allOf generation cannot be changed without forking; a post-process script keeps the TypeSpec source clean."
      },
      {
        "decision": "Vendored patches on dist/ of TypeSpec packages instead of forking",
        "rationale": "Forking would block upstream upgrades; minimal dist/ patches are reviewable and can be dropped when upstream fixes land."
      }
    ],
    "code_examples": [
      {
        "scenario": "Add a new v3 resource endpoint in the AIP package",
        "code": "// packages/aip/src/resources/myresource.tsp\nimport \"@typespec/http\";\nimport \"@typespec/rest\";\nusing TypeSpec.Http;\nusing TypeSpec.Rest;\n\nmodel MyResource {\n  id: string;\n  name: string;\n}\n\n// Register in packages/aip/src/openmeter.tsp:\n// import \"./resources/myresource.tsp\";\n// @route(\"/api/v1/myresources\")\n// interface MyResourceRoutes { ... }"
      }
    ]
  }
}