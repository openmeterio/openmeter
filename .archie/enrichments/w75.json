{
  "openmeter/billing/worker/subscriptionsync/service/reconciler/invoiceupdater": {
    "purpose": "Applies a typed patch set (create/update/delete lines and split-line-groups) to billing invoices during subscription sync reconciliation. It is the write-side of the reconciler: translates abstract diff patches into concrete billing.Service calls, branching on invoice type (gathering vs standard) and mutability.",
    "patterns": [
      {
        "name": "Sealed Patch discriminated union",
        "description": "Patch is a value type with a private op field. Callers construct via New*Patch() constructors and type-assert via As*Patch() methods that error on wrong op. Never add public fields to Patch or bypass As*Patch().",
        "example": "patch := NewUpdateLinePatch(line); update, err := patch.AsUpdateLinePatch() // errors if op != PatchOpLineUpdate"
      },
      {
        "name": "Invoice-type branch in ApplyPatches",
        "description": "After fetching the invoice, branch on invoice.Type() == billing.InvoiceTypeGathering first, then check StandardInvoice.StatusDetails.Immutable. Each branch calls a dedicated private method: updateGatheringInvoice, updateMutableStandardInvoice, or updateImmutableInvoice.",
        "example": "if invoice.Type() == billing.InvoiceTypeGathering { updateGatheringInvoice(...) } else if !standardInvoice.StatusDetails.Immutable { updateMutableStandardInvoice(...) } else { updateImmutableInvoice(...) }"
      },
      {
        "name": "EditFn closure for atomic invoice updates",
        "description": "All mutations to gathering and mutable-standard invoices go through billing.Service's UpdateGatheringInvoice / UpdateStandardInvoice with an EditFn closure. Never call low-level adapter methods directly.",
        "example": "u.billingService.UpdateStandardInvoice(ctx, billing.UpdateStandardInvoiceInput{Invoice: id, EditFn: func(invoice *billing.StandardInvoice) error { /* mutate */ return nil }})"
      },
      {
        "name": "Immutable invoice ValidationIssue accumulation",
        "description": "When the invoice is immutable, diff patches are not applied; instead they are collected as billing.ValidationIssue values with Severity=Warning and Code=ImmutableInvoiceHandlingNotSupportedErrorCode, then upserted via billing.Service.UpsertValidationIssues.",
        "example": "validationIssues = append(validationIssues, newValidationIssueOnLine(existingLine, \"flat fee line's per unit amount cannot be changed...\"))"
      },
      {
        "name": "Currency-grouped line provisioning",
        "description": "New gathering lines are grouped by currencyx.Code via lo.GroupBy before calling CreatePendingInvoiceLines once per currency group. billing.CreatePendingInvoiceLinesInput requires a single currency per call.",
        "example": "linesByCurrency := lo.GroupBy(lines, func(l billing.GatheringLine) currencyx.Code { return l.Currency }); for currency, lines := range linesByCurrency { billingService.CreatePendingInvoiceLines(ctx, ...) }"
      },
      {
        "name": "FlatFee helpers isolate price-type logic",
        "description": "feehelper.go exposes IsFlatFee, GetFlatFeePerUnitAmount, SetFlatFeePerUnitAmount to keep price-type branching out of invoiceupdate.go. Add new price-type helpers here, not inline in update logic.",
        "example": "if IsFlatFee(targetState) { existingAmt, _ := GetFlatFeePerUnitAmount(existingLine) }"
      },
      {
        "name": "New PatchOperation requires five coordinated changes",
        "description": "Adding a new PatchOperation requires: new constant, new payload struct, new As* accessor method, new New* constructor, new case in parsePatches switch, and new Log case. All five must be updated atomically.",
        "example": "const PatchOpLineCreate PatchOperation = \"line_create\" // + PatchLineCreate struct + AsCreateLinePatch() + NewCreateLinePatch() + parsePatches case + Log case"
      }
    ],
    "key_file_guides": [
      {
        "file": "invoiceupdate.go",
        "role": "Core Updater struct and ApplyPatches orchestration; owns all billing.Service call sites. Fetches invoices once per invoiceID in listInvoicesByID then distributes to type-specific handlers.",
        "watch_for": "Invoice fetch happens once per invoiceID inside the loop \u2014 do not add extra fetches. Empty-invoice deletion after mutable-standard update (when NonDeletedLineCount()==0) is intentional and must be preserved."
      },
      {
        "file": "patch.go",
        "role": "Patch value type, PatchOperation constants, New*/As* constructors/accessors, GetDeletePatchesForLine helper for building delete patches from a LineOrHierarchy.",
        "watch_for": "GetDeletePatchesForLine checks billing.AnnotationSubscriptionSyncIgnore and skips lines/groups with it set. Adding a new PatchOperation requires all five coordinated changes listed in patterns."
      },
      {
        "file": "feehelper.go",
        "role": "Flat-fee price-type helpers operating on billing.GenericInvoiceLineReader / billing.GenericInvoiceLine.",
        "watch_for": "SetFlatFeePerUnitAmount mutates the line in place via line.SetPrice(); callers must hold a mutable reference. Returns error if price type is not flat."
      }
    ],
    "anti_patterns": [
      "Calling billing.Adapter methods directly instead of going through billing.Service EditFn pattern",
      "Adding a new PatchOperation without updating all five locations: constant, struct, As*, New*, parsePatches switch (and Log case)",
      "Fetching the same invoice multiple times within one ApplyPatches call",
      "Producing ValidationIssues for mutable invoices \u2014 ValidationIssues are only for immutable invoice drift",
      "Using context.Background() instead of propagating the ctx parameter through all billing.Service calls"
    ],
    "decisions": [
      {
        "decision": "Patch is a sealed value type (private op, As* accessors) rather than an interface",
        "rationale": "Prevents callers from constructing invalid patch combinations and keeps the parsePatches switch exhaustive and compiler-checked within this package."
      },
      {
        "decision": "Immutable invoices receive ValidationIssues instead of hard errors",
        "rationale": "Invoice immutability is a billing state (e.g. issued/paid); reporting drift as warnings lets operators see subscription/invoice divergence without blocking the reconciler loop."
      },
      {
        "decision": "New lines are grouped by currency before CreatePendingInvoiceLines",
        "rationale": "billing.CreatePendingInvoiceLinesInput requires a single currency per call; grouping here prevents the caller from needing to pre-split lines."
      }
    ],
    "code_examples": [
      {
        "scenario": "Applying a mixed patch set to invoices of different types",
        "code": "import (\n\t\"context\"\n\t\"github.com/openmeterio/openmeter/openmeter/billing\"\n\t\"github.com/openmeterio/openmeter/openmeter/billing/worker/subscriptionsync/service/reconciler/invoiceupdater\"\n\t\"github.com/openmeterio/openmeter/openmeter/customer\"\n)\n\npatches := []invoiceupdater.Patch{\n\tinvoiceupdater.NewCreateLinePatch(newGatheringLine),\n\tinvoiceupdater.NewUpdateLinePatch(updatedLine),\n\tinvoiceupdater.NewDeleteLinePatch(lineID, invoiceID),\n}\nif err := updater.ApplyPatches(ctx, customer.CustomerID{Namespace: ns, ID: cid}, patches); err != nil {\n\treturn fmt.Errorf(\"applying patches: %w\", err)\n}"
      }
    ]
  }
}