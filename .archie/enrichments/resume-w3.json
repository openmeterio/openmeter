{
  "api/client/python/openmeter/aio": {
    "purpose": "Public async namespace for the OpenMeter Python SDK \u2014 exposes the async Client class and re-exports async operation symbols from the auto-generated Azure SDK layer (_generated/aio). Acts as a thin public shim: no business logic, only wiring the generated internals to a stable import surface.",
    "patterns": [
      {
        "name": "Thin shim over _generated",
        "description": "This package contains only __init__.py and _client.py. All async operation types come from _generated/aio; this folder's job is to re-export them under the public openmeter.aio namespace.",
        "example": "from .._generated.aio._client import OpenMeterClient  # in _client.py"
      },
      {
        "name": "Client subclasses OpenMeterClient",
        "description": "Client in _client.py extends the generated OpenMeterClient, injecting Bearer-token auth via ServiceKeyCredential + ServiceKeyCredentialPolicy when a token kwarg is supplied, and forwarding all other kwargs to super().__init__.",
        "example": "class Client(OpenMeterClient):\n    def __init__(self, endpoint='https://openmeter.cloud', token=None, **kwargs):\n        if token and not kwargs.get('authentication_policy'):\n            credential = ServiceKeyCredential(token)\n            kwargs['authentication_policy'] = policies.ServiceKeyCredentialPolicy(credential, 'Authorization', prefix='Bearer')\n        super().__init__(endpoint=endpoint, **kwargs)"
      },
      {
        "name": "_patch_sdk() call at module end",
        "description": "__init__.py must call _patch_sdk() as the last statement so runtime monkey-patches from _generated/aio/_patch.py are applied after all imports.",
        "example": "_patch_sdk()  # last line of __init__.py"
      },
      {
        "name": "Graceful _patch ImportError guard",
        "description": "_patch symbols are imported inside a try/except ImportError block so the package remains importable even if _patch.py is absent (e.g. before code-gen runs).",
        "example": "try:\n    from .._generated.aio._patch import __all__ as _patch_all\n    from .._generated.aio._patch import *\nexcept ImportError:\n    _patch_all = []"
      },
      {
        "name": "__all__ extension pattern",
        "description": "__all__ is seeded with public names defined here (Client), then extended with any names from _patch_all that are not already listed, preserving patch-introduced symbols.",
        "example": "__all__.extend([p for p in _patch_all if p not in __all__])"
      },
      {
        "name": "TYPE_CHECKING-only patch import",
        "description": "The _patch wildcard import for static type checkers is guarded under TYPE_CHECKING so it never executes at runtime, avoiding double-import side effects.",
        "example": "if TYPE_CHECKING:\n    from .._generated.aio._patch import *"
      }
    ],
    "key_file_guides": [
      {
        "file": "_client.py",
        "role": "Defines the public Client class \u2014 the sole hand-authored async client. Adds Bearer token auth injection on top of the generated OpenMeterClient base.",
        "watch_for": "Do not add operation methods here; they come from the generated base. If authentication_policy is already set in kwargs, the token is intentionally ignored \u2014 do not remove that guard."
      },
      {
        "file": "__init__.py",
        "role": "Assembles the public openmeter.aio namespace: imports Client, applies _patch overlay, extends __all__. Must not contain business logic.",
        "watch_for": "_patch_sdk() must be the last line; reordering breaks runtime patches. Wildcard import order matters: base symbols first, patch symbols second so patches override."
      }
    ],
    "anti_patterns": [
      "Defining new operation classes or methods directly in __init__.py or _client.py \u2014 all operations come from _generated",
      "Removing or relocating the _patch_sdk() call \u2014 runtime patches will silently not apply",
      "Manually editing files under _generated/aio/ \u2014 they are auto-generated and will be overwritten on next code-gen run",
      "Importing specific names from _generated instead of wildcard \u2014 breaks forward compatibility when _generated adds new operations",
      "Adding hard runtime imports of _patch symbols outside the try/except guard \u2014 causes ImportError when _patch.py is missing"
    ],
    "decisions": [
      {
        "decision": "Client subclasses generated OpenMeterClient rather than wrapping it",
        "rationale": "Inheritance lets all generated async operations be available on Client without explicit delegation; only auth injection and context-manager protocol need to be overridden."
      },
      {
        "decision": "Optional _patch.py overlay with graceful ImportError fallback",
        "rationale": "Allows monkey-patching generated behavior without requiring _patch.py to exist at all times, decoupling hand-authored patches from the code-gen cadence."
      }
    ],
    "code_examples": [
      {
        "scenario": "Instantiate the async client with a bearer token",
        "code": "from openmeter.aio import Client\n\nasync with Client(endpoint='https://openmeter.cloud', token='my-api-token') as client:\n    result = await client.meters.list()"
      }
    ]
  }
}