{
  "api/v3/handlers/customers/billing": {
    "purpose": "HTTP handlers for customer-scoped billing operations in the v3 API: reading/writing the customer billing profile override, app-specific customer data (Stripe, CustomInvoicing, Sandbox), and Stripe session management (checkout + portal). Primary constraint: all mutations must guard against deleted customers before calling domain services.",
    "patterns": [
      {
        "name": "HandlerWithArgs factory per operation",
        "description": "Each exported method on handler returns a typed httptransport.HandlerWithArgs[Request, Response, Params]. The decoder func resolves namespace, validates the customer is not deleted, then builds the domain input. The operation func calls the domain service. Error encoders are appended via httptransport.AppendOptions.",
        "example": "func (h *handler) GetCustomerBilling() GetCustomerBillingHandler { return httptransport.NewHandlerWithArgs(decoderFn, operationFn, commonhttp.JSONResponseEncoderWithStatus[...](http.StatusOK), httptransport.AppendOptions(h.options, ...)...) }"
      },
      {
        "name": "Deleted-customer guard in decoder",
        "description": "Every mutation decoder calls h.customerService.GetCustomer and checks cus.IsDeleted(), returning apierrors.NewGoneError if true. Skip this and deleted-customer writes go undetected.",
        "example": "if cus.IsDeleted() { return ..., apierrors.NewGoneError(ctx, errors.New(\"customer is deleted\")) }"
      },
      {
        "name": "App-type switch for customer data",
        "description": "Handlers that read or write app-specific data switch on application.GetType() over app.AppTypeStripe / AppTypeCustomInvoicing / AppTypeSandbox and return apierrors.NewInternalError for unknown types. Never add new app type handling in only one of the two sibling files (get_billing.go vs update_billing.go).",
        "example": "switch application.GetType() { case app.AppTypeStripe: ... case app.AppTypeCustomInvoicing: ... case app.AppTypeSandbox: ... default: return resp, apierrors.NewInternalError(ctx, fmt.Errorf(\"unsupported app type\")) }"
      },
      {
        "name": "Goverter for v3\u2192legacy enum conversions",
        "description": "convert.go declares goverter variable-set functions (FromAPI* / ToAPI*) with enum mapping annotations; convert.gen.go is generated. Never hand-edit convert.gen.go. Add new enum mappings to convert.go and run go generate.",
        "example": "// goverter:enum:map BillingAppStripeCheckoutSessionUIModeEmbedded CheckoutSessionUIModeEmbedded\nFromAPIBillingAppStripeCheckoutSessionUIMode func(source api.BillingAppStripeCheckoutSessionUIMode) (apilegacy.CheckoutSessionUIMode, error)"
      },
      {
        "name": "Domain-specific error encoder chained after generic",
        "description": "error_encoder.go returns a func that matches billing.NotFoundError \u2192 404, billing.ValidationError \u2192 400, billing.UpdateAfterDeleteError \u2192 409, billing.AppError \u2192 400. It is always appended after apierrors.GenericErrorEncoder() in httptransport.AppendOptions.",
        "example": "httptransport.WithErrorEncoder(apierrors.GenericErrorEncoder()), httptransport.WithErrorEncoder(errorEncoder())"
      },
      {
        "name": "Stripe app resolved via billing profile",
        "description": "Checkout session creation calls h.billingService.ResolveStripeAppIDFromBillingProfile to get the AppID; portal session creation uses h.billingService.GetCustomerApp with AppType filter. Do not hardcode app IDs.",
        "example": "appId, err := h.billingService.ResolveStripeAppIDFromBillingProfile(ctx, namespace, customerId)"
      }
    ],
    "key_file_guides": [
      {
        "file": "handler.go",
        "role": "Defines the Handler interface and the handler struct with its constructor New(). The handler holds billingService, customerService, stripeService but NOT appService (appService is declared in the struct but absent from New's signature \u2014 do not add it without updating the constructor).",
        "watch_for": "handler.appService field is declared but not set in New(); passing app.Service to New() will break the existing call sites."
      },
      {
        "file": "convert.go",
        "role": "Source of truth for all v3\u2194legacy\u2194domain type conversions; goverter annotations drive convert.gen.go generation. Run go generate after any change.",
        "watch_for": "Adding a new enum variant in TypeSpec without a matching goverter:enum:map annotation causes convert.gen.go to error on the unknown variant at runtime."
      },
      {
        "file": "convert.gen.go",
        "role": "Generated by goverter from convert.go. DO NOT EDIT.",
        "watch_for": "Any manual edit is overwritten on next go generate."
      },
      {
        "file": "error_encoder.go",
        "role": "Package-local error encoder matching billing-specific error types to HTTP codes. Must be appended AFTER GenericErrorEncoder so generic errors are caught first.",
        "watch_for": "Reversing the encoder order causes billing.ValidationError to fall through to 500 instead of 400."
      },
      {
        "file": "update_billing.go",
        "role": "Upserts both the billing profile override and app customer data atomically. If only app data should change, use update_billing_app_data.go instead.",
        "watch_for": "Both files share identical app-type switch logic; keep them in sync when adding a new app type."
      }
    ],
    "anti_patterns": [
      "Calling billing domain services without first checking customer.IsDeleted() in the decoder",
      "Hand-editing convert.gen.go instead of updating convert.go annotations and regenerating",
      "Appending errorEncoder() before apierrors.GenericErrorEncoder() (reverses precedence)",
      "Hardcoding a Stripe app ID instead of resolving it via billingService",
      "Adding app-type handling in only one of get_billing.go or update_billing.go"
    ],
    "decisions": [
      {
        "decision": "Stripe-specific session endpoints (checkout, portal) live in this package alongside generic billing profile endpoints",
        "rationale": "All endpoints share the same customer-scoped path prefix and require identical deleted-customer validation; collocating them avoids duplicated validation logic."
      },
      {
        "decision": "Goverter is used for v3-to-legacy enum conversions rather than hand-written switch statements",
        "rationale": "The enum space is large (10+ enums for Stripe checkout options); goverter with @error on unknown values gives compile-time safety and avoids manual mapping drift."
      }
    ],
    "code_examples": [
      {
        "scenario": "Add a new customer billing GET endpoint following the established pattern",
        "code": "type (\n\tFooRequest  = billing.GetFooInput\n\tFooResponse = api.BillingFoo\n\tFooParams   = string\n\tFooHandler  httptransport.HandlerWithArgs[FooRequest, FooResponse, FooParams]\n)\n\nfunc (h *handler) GetFoo() FooHandler {\n\treturn httptransport.NewHandlerWithArgs(\n\t\tfunc(ctx context.Context, r *http.Request, customerID FooParams) (FooRequest, error) {\n\t\t\tns, err := h.resolveNamespace(ctx)\n\t\t\tif err != nil { return FooRequest{}, err }\n\t\t\treturn FooRequest{CustomerID: customer.CustomerID{Namespace: ns, ID: customerID}}, nil\n\t\t},\n\t\tfunc(ctx context.Context, req FooRequest) (FooResponse, error) {\n\t\t\treturn h.billingService.GetFoo(ctx, req)\n\t\t},\n\t\tcommonhttp.JSONResponseEncoderWithStatus[FooResponse](http.StatusOK),\n\t\thttptransport.AppendOptions(h.options,\n\t\t\thttptransport.WithOperationName(\"get-foo\"),\n\t\t\thttptransport.WithErrorEncoder(apierrors.GenericErrorEncoder()),\n\t\t\thttptransport.WithErrorEncoder(errorEncoder()),\n\t\t)...,\n\t)\n}"
      }
    ]
  },
  "api/v3/handlers/customers/charges": {
    "purpose": "HTTP handler for listing a customer's billing charges (flat-fee and usage-based) in the v3 API. Primary constraint: credit-purchase charges are intentionally excluded at the query layer and must never appear in this API surface.",
    "patterns": [
      {
        "name": "HandlerWithArgs with inline pagination validation",
        "description": "ListCustomerCharges decoder builds a pagination.Page from query params with explicit page.Validate() that returns apierrors.NewBadRequestError on invalid page. Default page is (1, 20).",
        "example": "page := pagination.NewPage(1, 20)\nif args.Params.Page != nil { page = pagination.NewPage(lo.FromPtrOr(args.Params.Page.Number, 1), lo.FromPtrOr(args.Params.Page.Size, 20)) }\nif err := page.Validate(); err != nil { return ..., apierrors.NewBadRequestError(ctx, err, ...) }"
      },
      {
        "name": "Credit-purchase exclusion via ChargeTypes filter",
        "description": "The request always sets ChargeTypes: []meta.ChargeType{meta.ChargeTypeFlatFee, meta.ChargeTypeUsageBased}. convertChargeToAPI returns an explicit error if ChargeTypeCreditPurchase ever reaches it as a defensive measure.",
        "example": "req := ListCustomerChargesRequest{..., ChargeTypes: []meta.ChargeType{meta.ChargeTypeFlatFee, meta.ChargeTypeUsageBased}}"
      },
      {
        "name": "Realizations always expanded for booked totals",
        "description": "expands always includes meta.ExpandRealizations because booked totals require realization run data. RealtimeUsage is only added when the caller requests BillingChargesExpandRealTimeUsage.",
        "example": "expands := meta.Expands{meta.ExpandRealizations}\nif slices.Contains(*args.Params.Expand, api.BillingChargesExpandRealTimeUsage) { expands = expands.With(meta.ExpandRealtimeUsage) }"
      },
      {
        "name": "Sort field whitelist validation",
        "description": "Acceptable sort fields are \"id\", \"created_at\", \"service_period.from\", \"billing_period.from\". validChargesSortField rejects anything else with an explicit 400 listing allowed values.",
        "example": "if !validChargesSortField(sort.Field) { return ..., apierrors.NewBadRequestError(ctx, fmt.Errorf(\"unsupported sort field: %s\"), ...) }"
      },
      {
        "name": "Union-type dispatch in convertChargeToAPI",
        "description": "convertChargeToAPI switches on charge.Type(), calls AsFlatFeeCharge or AsUsageBasedCharge, maps to the API type, then calls out.FromBillingFlatFeeCharge or out.FromBillingUsageBasedCharge to set the discriminated union. Both the domain\u2192struct and struct\u2192union steps must succeed.",
        "example": "if err := out.FromBillingFlatFeeCharge(apiFF); err != nil { return out, fmt.Errorf(\"setting flat fee charge union: %w\", err) }"
      }
    ],
    "key_file_guides": [
      {
        "file": "handler.go",
        "role": "Defines Handler interface and handler struct. Constructor New() takes resolveNamespace + billingcharges.ChargeService. No billing.Service dependency \u2014 only the charges sub-service.",
        "watch_for": "Adding handler methods that need billing.Service will require extending the struct and constructor."
      },
      {
        "file": "list.go",
        "role": "Only handler in this package. Decodes pagination, sort, status filter, and expand from query params; calls service.ListCharges; maps results via convertChargeToAPI.",
        "watch_for": "Status filter parsing uses a type-safe switch in parseChargeStatusFilterSlice \u2014 add new status values there when meta.ChargeStatus gains new variants."
      },
      {
        "file": "convert.go",
        "role": "All domain\u2192API mapping functions. Many are exported (ConvertClosedPeriodToAPI, ConvertCurrencyCodeToAPI, etc.) and reused by other handler packages in the customers subtree.",
        "watch_for": "Exported converters are shared; changing their signatures affects callers in sibling packages (e.g., customerscredits)."
      }
    ],
    "anti_patterns": [
      "Including meta.ChargeTypeCreditPurchase in the ChargeTypes filter (credit purchases belong to the credits API)",
      "Omitting meta.ExpandRealizations from expands (breaks booked totals computation)",
      "Using a generic string sort field without validating against the whitelist in validChargesSortField",
      "Hand-writing the union set call (out.FromBillingFlatFeeCharge) without checking the error it returns"
    ],
    "decisions": [
      {
        "decision": "Credit-purchase charges are excluded from this endpoint and served by the credits API",
        "rationale": "Credit purchases have a distinct lifecycle (settlement, ledger funding) that belongs to the credits domain; mixing them into the charges list would conflate two different billing concepts."
      },
      {
        "decision": "Realization runs are always fetched even when not requested by the caller",
        "rationale": "Booked totals are computed from persisted realization runs; returning a charge without them would produce zero totals, which is incorrect for active charges."
      }
    ],
    "code_examples": []
  },
  "api/v3/handlers/customers/credits": {
    "purpose": "HTTP handlers for customer credit-grant lifecycle and balance queries in the v3 API: create/get/list grants, update external settlement status, get credit balance, and list ledger transactions. Primary constraint: this package is gated by credits.enabled; the handler is wired to noop ledger/balance services when credits are off.",
    "patterns": [
      {
        "name": "Local customerBalanceFacade interface",
        "description": "handler.go defines a narrow customerBalanceFacade interface (GetBalance, GetBalances, ListCreditTransactions) rather than importing the full customerbalance service. This keeps the dependency surface explicit and testable.",
        "example": "type customerBalanceFacade interface {\n\tGetBalance(ctx context.Context, input customerbalance.GetBalanceInput) (alpacadecimal.Decimal, error)\n\tGetBalances(ctx context.Context, input customerbalance.GetBalancesInput) ([]customerbalance.BalanceByCurrency, error)\n\tListCreditTransactions(ctx context.Context, input customerbalance.ListCreditTransactionsInput) (customerbalance.ListCreditTransactionsResult, error)\n}"
      },
      {
        "name": "Settlement-type dispatch for purchase block",
        "description": "toAPICreditGrantPurchase switches on settlement.Type() (invoice, external, promotional). Promotional grants return nil purchase. Unknown types return an error. The outer toAPIBillingCreditGrant handles the nil case gracefully.",
        "example": "switch settlement.Type() {\ncase creditpurchase.SettlementTypePromotional: return nil, nil\ncase creditpurchase.SettlementTypeInvoice: ...\ncase creditpurchase.SettlementTypeExternal: ...\ndefault: return nil, fmt.Errorf(\"invalid settlement type\")\n}"
      },
      {
        "name": "Base64-JSON cursor for transaction pagination",
        "description": "encodeBillingCreditTransactionCursor/decodeBillingCreditTransactionCursor marshal a {booked_at, created_at, id} struct to JSON then base64. Cursors are validated after decoding via ledgerCursor.Validate().",
        "example": "raw, _ := json.Marshal(payload)\nreturn base64.StdEncoding.EncodeToString(raw), nil"
      },
      {
        "name": "models.ValidationIssue for domain-specific errors",
        "description": "errors.go uses models.NewValidationIssue with a typed ErrorCode constant, WithCriticalSeverity, WithHTTPStatusCodeAttribute(400), and WithFieldString to produce structured errors that apierrors.GenericErrorEncoder handles without a custom error encoder in this package.",
        "example": "return models.NewValidationIssue(errCodeCreditGrantExternalSettlementStatusInvalid, fmt.Sprintf(\"unsupported...\"), models.WithCriticalSeverity(), commonhttp.WithHTTPStatusCodeAttribute(http.StatusBadRequest), models.WithFieldString(\"status\"))"
      },
      {
        "name": "Status-to-domain conversion with explicit unknown handling",
        "description": "fromAPIBillingCreditGrantStatus maps API status strings to meta.ChargeStatus. 'expired' maps to ChargeStatusFinal (terminal). Unknown values return a plain fmt.Errorf (not a ValidationIssue) \u2014 the caller wraps it in apierrors.NewBadRequestError.",
        "example": "case api.BillingCreditGrantStatusExpired:\n\treturn meta.ChargeStatusFinal, nil"
      }
    ],
    "key_file_guides": [
      {
        "file": "handler.go",
        "role": "Defines Handler interface (6 methods) and handler struct with ledger + accountResolver in addition to the balance facade and creditGrantService. New() wires all of them.",
        "watch_for": "ledger and accountResolver are wired to noops by app/common when credits.enabled=false; handlers must not assume they are real implementations."
      },
      {
        "file": "convert.go",
        "role": "All domain\u2194API mapping. toAPIBillingCreditGrant is the primary domain\u2192API converter; fromAPICreateCreditGrantRequest is the primary API\u2192domain converter.",
        "watch_for": "fromAPICreateCreditGrantRequest returns an error for feature filters (not yet supported); add a TODO if that changes."
      },
      {
        "file": "errors.go",
        "role": "Defines newCreditGrantExternalSettlementStatusInvalid using models.NewValidationIssue with HTTP status attribute, allowing GenericErrorEncoder to produce a 400 without a package-local error encoder.",
        "watch_for": "Other packages in customers/* use a local errorEncoder(); this package intentionally does not. Keep it consistent."
      },
      {
        "file": "externalsettlement.go",
        "role": "Handler for PATCH /customers/{id}/credits/{grant}/external-settlement. Calls creditGrantService.UpdateExternalSettlement. Only authorized\u2192settled transitions are valid; pending is rejected in fromAPIBillingCreditPurchasePaymentSettlementStatus.",
        "watch_for": "The 'pending' status must remain rejected; a grant cannot be downgraded to pending."
      },
      {
        "file": "get_balance.go",
        "role": "Returns per-currency credit balances. Rejects feature filter queries (unsupported). Calls customerService.GetCustomer first to validate existence before querying balanceFacade.",
        "watch_for": "The feature filter rejection is a hard constraint; do not silently ignore the filter."
      }
    ],
    "anti_patterns": [
      "Assuming ledger or accountResolver are real implementations \u2014 they may be noops when credits.enabled=false",
      "Adding a package-local errorEncoder() \u2014 this package relies on models.ValidationIssue + WithHTTPStatusCodeAttribute for error encoding",
      "Allowing the 'pending' settlement status to succeed in fromAPIBillingCreditPurchasePaymentSettlementStatus",
      "Returning a non-nil purchase block for SettlementTypePromotional grants",
      "Using context.Background() instead of the ctx passed to the handler operation"
    ],
    "decisions": [
      {
        "decision": "customerBalanceFacade is a local interface rather than importing the full customerbalance.Service",
        "rationale": "Narrows the dependency surface to only the three methods this handler needs, making tests easier to mock and preventing accidental use of write operations."
      },
      {
        "decision": "ValidationIssue with WithHTTPStatusCodeAttribute replaces a custom errorEncoder for domain validation errors",
        "rationale": "apierrors.GenericErrorEncoder already inspects the HTTP status attribute on ValidationIssues; adding a second encoder would be redundant and could cause double-handling."
      }
    ],
    "code_examples": []
  }
}