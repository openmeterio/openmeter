{
  "openmeter/subject/service/hooks": {
    "purpose": "Implements the CustomerSubjectHook that bridges customer.Service lifecycle events to subject.Service provisioning, ensuring every UsageAttribution.SubjectKey has a corresponding Subject row without creating circular imports between the subject and customer domains.",
    "patterns": [
      {
        "name": "Embed NoopServiceHook for partial overrides",
        "description": "Hook structs embed NoopCustomerSubjectHook = models.NoopServiceHook[customer.Customer] and override only the needed methods (PostCreate, PostUpdate); other lifecycle methods default to no-ops.",
        "example": "type customerSubjectHook struct { NoopCustomerSubjectHook; provisioner *SubjectProvisioner; tracer trace.Tracer }"
      },
      {
        "name": "Context-key skip flag prevents re-entrant loops",
        "description": "NewContextWithSkipSubjectCustomer sets a typed private context key; any call back into subject.Service inside a hook must carry it to prevent infinite recursion when a reverse hook is also registered.",
        "example": "sub, err = p.subject.Create(NewContextWithSkipSubjectCustomer(ctx), subject.CreateInput{...})"
      },
      {
        "name": "Config struct with Validate() for constructor injection",
        "description": "Dependencies (Subject service, Logger, Tracer) are bundled in SubjectProvisionerConfig (implements models.Validator); constructors call config.Validate() before allocation.",
        "example": "func NewCustomerSubjectHook(config CustomerSubjectHookConfig) (CustomerSubjectHook, error) { if err := config.Validate(); err != nil { return nil, fmt.Errorf(\"invalid subject hook config: %w\", err) } ... }"
      },
      {
        "name": "OTel span per operation with deferred err capture",
        "description": "Every exported method opens a tracer span at entry and defers span.End() with status-setting in a closure capturing the named error; RecordError only on non-nil err.",
        "example": "ctx, span := p.tracer.Start(ctx, \"subject_provisioner.ensure_subjects\"); defer func() { if err != nil { span.SetStatus(otelcodes.Error, err.Error()); span.RecordError(err) }; span.End() }()"
      },
      {
        "name": "models.IsGenericNotFoundError for idempotent upsert",
        "description": "EnsureSubject calls GetByIdOrKey and treats IsGenericNotFoundError (or a deleted subject) as the signal to create; all other errors propagate. Never match by string comparison.",
        "example": "if models.IsGenericNotFoundError(err) || sub.IsDeleted() { sub, err = p.subject.Create(NewContextWithSkipSubjectCustomer(ctx), ...) }"
      },
      {
        "name": "Delegate logic to SubjectProvisioner, not hook methods",
        "description": "Hook lifecycle methods are thin delegators calling provisioner.EnsureSubjects and handling span status; all provisioning logic lives in SubjectProvisioner so it can be reused outside the hook interface.",
        "example": "func (s customerSubjectHook) PostCreate(ctx context.Context, cus *customer.Customer) error { ...; return s.provisioner.EnsureSubjects(ctx, cus) }"
      }
    ],
    "key_file_guides": [
      {
        "file": "customersubject.go",
        "role": "Sole production file: customerSubjectHook (models.ServiceHook[customer.Customer]), SubjectProvisioner, and config structs. Type alias CustomerSubjectHookConfig = SubjectProvisionerConfig keeps the public API minimal.",
        "watch_for": "Any new lifecycle method must pass NewContextWithSkipSubjectCustomer(ctx) when calling back into subject.Service. Never skip config.Validate() in constructors. Preserve the EnsureSubjects nil-guard on cus."
      },
      {
        "file": "customersubject_test.go",
        "role": "Integration tests wiring concrete adapters (subjectadapter, customeradapter, customerservice, subjectservice) without app/common. Covers WithExistingSubject, WithoutExistingSubject, and mutual Conflict scenarios.",
        "watch_for": "Tests use t.Context(). testutils.InitPostgresDB + DBSchemaMigrate must run before any DB interaction. NoopCustomerOverrideService stubs billing deps without importing billing wiring."
      }
    ],
    "anti_patterns": [
      "Calling subject.Service inside a hook without NewContextWithSkipSubjectCustomer when a reverse hook is registered \u2014 causes infinite recursion.",
      "Importing app/common in tests \u2014 breaks isolation; build test deps from concrete constructors directly.",
      "Using context.Background() instead of propagating the caller ctx through hook methods.",
      "Catching subject.Service errors by string comparison instead of models.IsGenericNotFoundError.",
      "Adding business logic in hook lifecycle methods instead of delegating to SubjectProvisioner."
    ],
    "decisions": [
      {
        "decision": "SubjectProvisioner is separate from customerSubjectHook.",
        "rationale": "The provisioner can be reused for non-hook paths (e.g. batch backfills) without the models.ServiceHook interface overhead."
      },
      {
        "decision": "Skip flag lives in context rather than as a boolean field on the hook.",
        "rationale": "A context key is scoped to one call stack, preventing skip state from leaking across concurrent requests sharing the same hook instance."
      },
      {
        "decision": "Type alias CustomerSubjectHookConfig = SubjectProvisionerConfig.",
        "rationale": "Avoids duplicating config fields while letting callers use the hook-specific name; a single Validate() covers both entry points."
      }
    ],
    "code_examples": [
      {
        "scenario": "Adding a PostDelete lifecycle that prevents reverse-hook loops",
        "code": "import (\n\t\"context\"\n\t\"github.com/openmeterio/openmeter/openmeter/customer\"\n\tsubjectservicehooks \"github.com/openmeterio/openmeter/openmeter/subject/service/hooks\"\n)\n\nfunc (s customerSubjectHook) PostDelete(ctx context.Context, cus *customer.Customer) error {\n\tctx, span := s.tracer.Start(ctx, \"customer_subject_hook.post_delete\")\n\tdefer span.End()\n\tctx = subjectservicehooks.NewContextWithSkipSubjectCustomer(ctx)\n\treturn s.provisioner.RemoveSubjects(ctx, cus)\n}"
      }
    ]
  }
}